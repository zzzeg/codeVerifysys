# 需求文档更新说明 v1.3

## 更新时间
2025-12-12

## 主要更新

### 1. ❌ 删除的内容

#### 1.1 删除"10. 高级功能"章节
移除了以下功能模块，简化系统设计：
- WebSocket 实时通信
- 打印功能
- 页面水印
- 新手引导页

这些功能被评估为非核心业务需求，可在后期根据实际需要再行添加。

---

### 2. ✅ 新增的内容

#### 2.1 系统角色定义（1.5 章节）

**管理员角色 (Administrator)**
- **角色标识**: `admin`
- **完整权限**: 拥有所有系统功能访问权限
- **核心职责**:
  - 管理所有用户和角色
  - 控制用户权限和页面访问
  - 配置系统参数
  - 查看所有用户的操作日志
  
**普通用户角色 (User)**
- **角色标识**: `user`
- **默认角色**: 新用户注册时自动分配
- **业务权限**: 仅能访问业务功能页面
- **可访问页面**:
  - ✅ 注册码生成
  - ✅ 注册码列表管理
  - ✅ 项目管理
  - ✅ 自定义数据
  - ✅ 自动发卡
  - ✅ 个人中心

**受限功能**:
  - ❌ 用户管理
  - ❌ 角色管理
  - ❌ 菜单管理
  - ❌ 部门管理
  - ❌ 系统配置
  - ❌ 系统日志（仅可见自己的）

**角色管理规则**:
- 只有管理员可以提升或降级用户角色
- 系统至少保留一个管理员账号
- 每个用户只能拥有一个角色（暂不支持多角色）
- 所有角色变更都会记录在操作日志中

---

#### 2.2 业务功能页面（6.7 章节）

##### 6.7.1 注册码生成
**功能概述**: 生成系统激活码用于用户授权

**核心能力**:
- 批量生成注册码
- 设置有效期（天数）
- 限制使用次数（单次/多次）
- 绑定到特定项目
- 添加备注信息
- 导出注册码列表（Excel/CSV）
- 批量操作支持

**使用场景**:
- 用户需要为客户批量生成激活码
- 不同项目需要不同配置的注册码
- 需要跟踪注册码的生成记录

##### 6.7.2 注册码列表管理
**功能概述**: 管理所有生成的注册码

**核心能力**:
- 表格形式展示所有注册码
- 按状态筛选（未使用/已使用/已过期/已禁用）
- 多条件搜索（注册码/项目名称/创建人）
- 查看注册码详细信息和使用历史
- 状态管理（启用/禁用/延期）
- 批量操作（批量禁用/删除）
- 分页加载支持大数据量

**使用记录跟踪**:
- 使用时间
- 使用者信息
- IP 地址
- 设备信息

##### 6.7.3 项目管理
**功能概述**: 管理业务项目，注册码可分类到项目

**核心能力**:
- 项目CRUD操作
- 项目统计（注册码总数/已使用/未使用/已过期）
- 查看项目下所有注册码
- 项目启用/禁用状态控制

**项目信息**:
- 项目名称（必填）
- 项目描述
- 项目图标
- 项目状态

##### 6.7.4 自定义数据
**功能概述**: 灵活的数据管理系统，用户可自定义数据结构

**核心能力**:
- 创建自定义数据模板
- 定义多种字段类型（文本/数字/日期/下拉选项等）
- 设置字段验证规则
- 基于模板录入数据
- 批量导入（Excel）
- 数据导出（Excel/CSV）
- 权限隔离（用户只能管理自己的数据）

**应用场景**:
- 用户需要存储特定业务数据
- 数据结构因业务而异
- 需要灵活的数据管理方案

##### 6.7.5 自动发卡
**功能概述**: 自动化发放注册码功能

**核心能力**:
- 发卡规则配置
  - 时间触发
  - 数量触发
  - 手动触发
- 手动立即发卡
- 定时发卡（可选功能）
- 发卡记录和日志
- 发卡统计（按项目/按时间）

**配置项**:
- 发放项目
- 单次发放数量
- 注册码有效期
- 触发条件

---

#### 2.3 API 端点扩展

新增业务功能相关的 API 端点：

**注册码管理 API** (12个端点):
```
GET    /api/codes                  # 获取注册码列表
POST   /api/codes/generate         # 生成注册码
POST   /api/codes/batch-generate   # 批量生成
PATCH  /api/codes/:id/status       # 更改状态
PATCH  /api/codes/:id/extend       # 延长有效期
DELETE /api/codes/:id              # 删除
... 等
```

**项目管理 API** (7个端点):
```
GET    /api/projects               # 获取项目列表
POST   /api/projects               # 创建项目
PUT    /api/projects/:id           # 更新项目
... 等
```

**自定义数据 API** (12个端点):
```
GET    /api/custom-data/templates      # 模板列表
POST   /api/custom-data/templates      # 创建模板
GET    /api/custom-data/records        # 数据记录
POST   /api/custom-data/import         # 批量导入
... 等
```

**自动发卡 API** (9个端点):
```
GET    /api/auto-issue/rules       # 规则列表
POST   /api/auto-issue/rules       # 创建规则
POST   /api/auto-issue/execute     # 手动触发
... 等
```

---

#### 2.4 数据库表设计

新增 8 个业务功能数据表：

1. **verify_codes** - 注册码表
2. **verify_code_usage** - 注册码使用记录表
3. **verify_projects** - 项目表
4. **verify_custom_templates** - 自定义数据模板表
5. **verify_custom_records** - 自定义数据记录表
6. **verify_auto_issue_rules** - 自动发卡规则表
7. **verify_auto_issue_records** - 自动发卡记录表

所有表名统一使用 `verify_` 前缀，符合项目命名规范。

**关键字段说明**:
- JSON 字段用于存储灵活配置（fields, data, trigger_config）
- created_by 记录创建人，用于数据权限隔离
- timestamps 标准时间戳字段（created_at, updated_at）

---

## 系统架构变化

### 权限模型简化
从原来较为复杂的权限模型简化为**二级权限模型**：
- **管理员**: 完全控制权
- **普通用户**: 业务功能权限

这种简化使得：
1. 权限管理更加清晰
2. 系统实现更加简单
3. 适合中小型业务场景

### 功能聚焦
移除非核心功能后，系统更加聚焦于：
1. **注册码管理**: 核心业务功能
2. **项目分类**: 组织管理能力
3. **灵活数据**: 可扩展性
4. **自动化**: 提升效率

---

## 开发影响

### 前端开发
需要新开发的页面：
- [ ] 注册码生成页面
- [ ] 注册码列表页面
- [ ] 项目管理页面
- [ ] 自定义数据模板页面
- [ ] 自定义数据录入页面
- [ ] 自动发卡配置页面

### 后端开发
需要开发的 API 模块：
- [ ] 注册码管理模块 (12个API)
- [ ] 项目管理模块 (7个API)
- [ ] 自定义数据模块 (12个API)
- [ ] 自动发卡模块 (9个API)

### 数据库开发
需要创建的数据表：
- [ ] 8个业务功能表
- [ ] 相关索引和外键约束
- [ ] 数据库迁移脚本

---

## 技术实现要点

### 1. 注册码生成算法
建议使用以下方案：
- UUID v4 生成唯一标识
- 自定义编码规则（如：项目前缀 + 随机字符）
- 防重复校验

### 2. 自定义数据存储
使用 JSON 字段存储灵活数据：
```json
{
  "fields": [
    {"name": "姓名", "type": "text", "required": true},
    {"name": "年龄", "type": "number", "min": 0, "max": 150}
  ]
}
```

### 3. 自动发卡触发
- 时间触发：使用 node-cron 定时任务
- 数量触发：在注册码使用时检测
- 手动触发：直接 API 调用

### 4. 权限控制
- 使用中间件验证用户角色
- 数据隔离（普通用户只能看到自己的数据）
- 管理员可查看所有数据

---

## 测试要点

### 功能测试
- [ ] 注册码生成和使用流程
- [ ] 项目管理 CRUD
- [ ] 自定义数据模板和数据录入
- [ ] 自动发卡规则触发
- [ ] 权限控制（管理员 vs 普通用户）

### 性能测试
- [ ] 大量注册码生成性能
- [ ] 注册码列表分页性能
- [ ] 自定义数据查询性能

### 安全测试
- [ ] 越权访问测试
- [ ] 注册码重复性测试
- [ ] 数据隔离测试

---

## 迁移指南

如果从 v1.2 升级到 v1.3:

### 数据库迁移
```sql
-- 创建新业务表
CREATE TABLE verify_codes (...);
CREATE TABLE verify_projects (...);
-- ... 其他表

-- 添加索引
CREATE INDEX idx_codes_project ON verify_codes(project_id);
CREATE INDEX idx_codes_status ON verify_codes(status);
```

### 代码迁移
- 移除 WebSocket、打印、水印相关代码
- 添加新的业务功能路由
- 更新权限配置

---

## 后续规划

### v1.4 计划
- 实现所有业务功能前端页面
- 完成所有业务功能后端 API
- 完善权限控制逻辑

### v2.0 展望
- 支持多角色（自定义角色）
- WebSocket 实时通知（可选恢复）
- 高级统计报表
- 数据看板

---

**文档版本**: v1.3  
**更新日期**: 2025-12-12  
**维护人员**: AI Assistant
