# 需求文档更新说明 v1.2

## 更新时间
2025-12-11

## 更新内容概览

### 1. 📊 数据库配置明确化

#### 本地开发环境配置
已在文档中明确添加 MySQL 数据库配置信息：

```yaml
数据库类型: MySQL 8.0
主机地址: 127.0.0.1
端口: 3306
数据库名: dbs
用户名: root
密码: （无）
表名前缀: verify_
字符集: utf8mb4
排序规则: utf8mb4_unicode_ci
```

#### 数据库命名规范
- **统一表名前缀**: 所有数据表统一使用 `verify_` 前缀
- **命名规则**: 表名使用小写字母和下划线
- **表名形式**: 采用复数形式（如 verify_users）

#### 已更新的数据表名称
所有核心数据表已更新为带前缀的格式：

1. `verify_users` - 用户表
2. `verify_roles` - 角色表
3. `verify_permissions` - 权限表
4. `verify_user_roles` - 用户角色关联表
5. `verify_role_permissions` - 角色权限关联表
6. `verify_departments` - 部门表
7. `verify_operation_logs` - 操作日志表
8. `verify_login_logs` - 登录日志表

### 2. 🔧 技术栈调整

#### 包管理器
- 从 `pnpm` 调整为 `npm`（前后端统一）

#### Web 框架
- 明确使用 `Express.js`（去除 Koa 选项）

#### 数据库
- 明确使用 `MySQL 8.0`（去除 PostgreSQL 选项）

### 3. 🏷️ 可选功能标注

以下涉及第三方付费服务的功能已标注为【可选】，初始版本暂不接入：

#### 云存储服务
- AWS S3
- 七牛云
- 阿里云 OSS

**替代方案**: 使用本地文件存储

#### 实时通信
- WebSocket (Socket.io)

#### 邮件服务
- nodemailer

#### 短信服务
- 阿里云短信 SDK
- 腾讯云短信 SDK

#### 错误监控
- Sentry

#### 其他可选
- Redis（标注为可选，可使用内存缓存替代）

### 4. 📝 文档说明补充

在"通信与实时功能"章节添加了重要说明：

> **注意**: 以上标注为【可选】的功能涉及第三方付费服务，初始版本暂不接入，预留接口供后期扩展使用。

## 技术实现建议

### 文件上传
初始版本使用本地文件存储：
- 使用 `multer` 处理文件上传
- 使用 `sharp` 进行图片处理
- 文件存储在服务器本地目录（如 `/uploads`）
- 预留云存储接口，便于后期切换

### 缓存方案
如不使用 Redis，可选用：
- 内存缓存（适合单机部署）
- 文件缓存
- Session 存储使用 express-session + file-store

### 实时通知
初期可使用轮询方式：
- 定时请求接口获取最新通知
- 前端定时刷新数据
- 后期再升级为 WebSocket 实时推送

## 开发优先级

### 第一阶段（必需）
✅ MySQL 数据库连接
✅ 本地文件上传
✅ JWT 认证
✅ RBAC 权限管理
✅ 基础 API 接口

### 第二阶段（可选，后期扩展）
⏸️ Redis 缓存
⏸️ 云存储服务
⏸️ WebSocket 实时通信
⏸️ 邮件通知
⏸️ 短信验证码
⏸️ Sentry 错误监控

## 环境变量配置示例

创建 `.env.example` 文件：

```env
# 服务器配置
NODE_ENV=development
PORT=3000

# 数据库配置
DB_TYPE=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=dbs
DB_USERNAME=root
DB_PASSWORD=
DB_TABLE_PREFIX=verify_

# JWT 配置
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=7d

# 文件上传配置
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

# Redis 配置（可选）
# REDIS_HOST=127.0.0.1
# REDIS_PORT=6379
# REDIS_PASSWORD=

# 云存储配置（可选）
# OSS_ACCESS_KEY_ID=
# OSS_ACCESS_KEY_SECRET=
# OSS_BUCKET=
# OSS_REGION=

# 邮件配置（可选）
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=

# 短信配置（可选）
# SMS_ACCESS_KEY_ID=
# SMS_ACCESS_KEY_SECRET=
```

## 注意事项

1. **数据库初始化**: 需要手动创建 `dbs` 数据库
2. **表结构同步**: 使用 ORM 的 migration 功能创建数据表
3. **文件权限**: 确保上传目录有写入权限
4. **安全配置**: 生产环境必须修改 JWT_SECRET
5. **跨域配置**: 开发环境需要配置 CORS

## 后续计划

- v1.3: 开始后端项目搭建
- v1.4: 完成数据库设计和建表
- v1.5: 完成核心 API 开发
- v2.0: 前后端联调完成

---

**文档版本**: v1.2  
**更新日期**: 2025-12-11  
**维护人员**: AI Assistant
