# 需求文档更新说明 v1.4

## 更新时间
2025-12-12

## 概述

v1.4 是一次重大的业务功能重构更新，根据实际业务需求对注册码管理、项目管理、自定义数据和自动发卡等核心功能进行了全面优化和重新设计，新增了安全策略模块和商品系统。

---

## 一、注册码管理重构

### 1.1 注册码生成功能升级

#### 新增卡类型系统
引入了完整的卡类型体系，支持多种时长规格：
- **试用卡** (trial)
- **小时卡** (hour) 
- **天卡** (day) - 按24小时计算
- **周卡** (week) - 按7天计算
- **月卡** (month) - 按30自然日计算
- **季卡** (quarter) - 90天
- **半年卡** (half_year) - 180天
- **年卡** (year) - 365天
- **永久卡** (permanent) - 实际为10年

#### 生成流程优化
- 选择项目后生成注册码
- 支持批量生成（可选择生成数量）
- 生成后显示结果表格（注册码、项目名称、卡类型、备注）
- 提供导出功能（Excel/TXT格式）
- 记录生成人信息（普通用户不可见，管理员可见）

### 1.2 注册码列表管理增强

#### 新增字段
- **机器码**: 绑定的设备机器码
- **解绑密码**: 用于解绑设备
- **是否在线**: 实时在线状态
- **激活时间**: 首次激活时间
- **到期时间**: 注册码到期时间
- **最后访问时间**: 最近一次登录时间
- **销售状态**: 作者生成 / 自动发卡

#### 多维度筛选
- **状态筛选**: 未使用/使用中/已过期/已删除/已冻结
- **卡类型筛选**: 试用卡、小时卡、天卡、周卡、月卡、季卡、半年卡、年卡、永久
- **项目筛选**: 下拉选择用户创建的项目
- **备注搜索**: 模糊查询
- **在线状态**: 所有/在线/离线
- **销售状态**: 所有/作者生成/自动发卡
- **时间范围**: 可选择时间类型（激活时间/到期时间/最后登录时间）

#### 扩展操作功能
单个操作：
- 解绑：解除机器码绑定
- 冻结：停用注册码
- 续费：延长有效期
- 下线：强制下线
- 删除：删除注册码

批量操作：
- 批量冻结/解冻
- 批量解绑
- 批量续费
- 批量删除/恢复
- 批量导入（TXT/Excel）
- 批量导出（TXT/Excel）
- 清理已过期
- 批量修改备注
- 批量转换项目
- 批量添加IP到黑名单

#### 详情查看
底部弹窗显示完整信息：
- 注册码、项目名称
- 解绑密码、卡类型
- 创建时间、激活时间、到期时间、最后登录时间
- 状态、是否在线
- 登录IP地址
- 是否绑定、机器码
- 使用者信息
- 备注（可修改）

---

## 二、项目管理系统化

### 2.1 新增核心配置

#### 试用模式
- **试用（试用时间）**: 设置试用时长
- **试用（解绑扣时）**: 解绑时扣除时间
- **禁止试用**: 不允许试用

#### 机器码绑定管理
- **开启机器码绑定**: 启用/关闭
- **用户自行解绑**: 是否允许用户自己解绑
- **顶号功能**: 
  - 开启：同一注册码在新地方登录，旧地方自动掉线
  - 关闭：一个注册码登录后，不能在其他地方登录，除非调用下线函数或手动下线

#### 安全配置
- **解绑密码**: 最大8个字符
- **项目备注**: 最长1024个字符
- **项目公告**: 最长1024个字符

### 2.2 删除确认机制
删除项目时：
- 二次确认提示
- 说明：删除项目后，项目下所有注册码将被删除
- 说明：项目下所有自定义数据将被删除

### 2.3 关联管理
- 查看项目下所有注册码
- 查看项目下自定义数据
- 项目统计数据（注册码总数、已使用、未使用、已过期）

---

## 三、自定义数据简化

### 3.1 从模板系统转为 Key-Value 模式

**原设计**（v1.3）:
- 创建数据模板
- 定义字段类型
- 基于模板录入数据
- 复杂且冗余

**新设计**（v1.4）:
- 简化为 Key-Value 键值对
- 绑定到项目
- 直接创建和管理
- 更灵活、更简单

### 3.2 数据管理
- **创建数据**: 选择项目、输入Key、Value、备注
- **编辑数据**: 修改Key、Value、备注
- **删除数据**: 二次确认
- **搜索筛选**: 按项目名称、Key值、备注

---

## 四、新增：安全策略模块

### 4.1 功能概述
为每个项目配置独立的加密安全策略，保护客户端与服务器之间的通信安全。

### 4.2 策略模式

#### 初级模式（Basic）
简化配置，适合快速部署：
- **用户密钥**: 客户端解密密钥（最大32字符）
- **验证接口安全策略**: 
  - 通讯加密算法（下拉选择）: AES/DES/TEA/RC2/RC5/RC6/BLOWFISH
- **辅助接口安全策略**:
  - 数据加密算法（下拉选择）: MD5/SHA1/SHA256/SHA384/SHA512

#### 高级模式（Advanced）
完全自定义配置，适合高安全要求：
- **用户密钥**: 客户端解密密钥（最大32字符）
- **用户密钥加密**: AES/DES
- **验证接口安全策略**:
  - 通讯加密：列出所有加密算法，用户自定义算法标识
  - 加密后数据前缀（最大32字符）
  - 加密后数据后缀（最大32字符）
- **辅助接口安全策略**:
  - 通讯加密：列出所有加密算法，用户自定义算法标识
  - 加密后数据前缀
  - 加密后数据后缀

### 4.3 管理功能
- CRUD 操作（创建、查看、编辑、删除）
- 按项目筛选
- 按策略模式筛选（初级/高级）
- 按策略状态筛选（开启/关闭）
- 二次确认删除

---

## 五、自动发卡重构为商品系统

### 5.1 从发卡规则到商品购买

**原设计**（v1.3）:
- 配置发卡规则
- 触发条件（时间/数量/手动）
- 自动发放注册码

**新设计**（v1.4）:
- 创建商品
- 生成购买链接
- 客户购买付款
- 自动生成并发放注册码

### 5.2 商品管理

#### 商品添加
- **选择项目**: 绑定到具体项目
- **商品名称**: 必填
- **商品简介**: 简短描述
- **匿名购买**: 启用后无需注册即可购买
- **商品类型/价格**: 表格形式管理
  - 商品类型（自定义名称，如"优惠版"、"标准版"）
  - 价格（必填）
  - 卡类型（小时卡、天卡、周卡、月卡、季卡、半年卡、年卡、永久卡）
  - 支持添加/编辑/删除一行
- **购买数量限制**:
  - 单次最少购买数量
  - 单次最多购买数量
- **商品描述**: 最大1024字符，支持换行

#### 商品列表
表格显示字段：
- 项目名称
- 商品名称
- 购买数（最少/最多）
- 允许匿名购买（允许/不允许）
- 商品链接（可直接点击跳转）
- 商品简介
- 操作（编辑、删除）

#### 商品操作
- **编辑商品**: 修改商品信息
- **删除商品**: 二次确认
- **商品搜索**: 按商品名称（模糊查询）、项目名称（下拉选择）

### 5.3 发卡统计（仅管理员可见）
- 总发卡次数
- 总发卡数量
- 按项目统计
- 按时间统计

---

## 六、数据库设计变更

### 6.1 新增数据表

1. **verify_security_policies** - 安全策略表
2. **verify_products** - 商品表
3. **verify_product_types** - 商品类型价格表  
4. **verify_orders** - 订单表
5. **verify_ip_blacklist** - IP黑名单表
6. **verify_machine_blacklist** - 机器码黑名单表
7. **verify_generation_batches** - 生成批次记录表

### 6.2 修改的数据表

#### verify_codes（注册码表）
新增字段：
- card_type: 卡类型
- unbind_password: 解绑密码
- machine_code: 机器码
- is_online: 是否在线
- is_bound: 是否已绑定
- last_login_ip: 最后登录IP
- last_login_at: 最后登录时间
- activated_at: 激活时间
- expired_at: 到期时间
- sale_type: 销售状态
- deleted_at: 软删除时间

#### verify_projects（项目表）
新增字段：
- trial_mode: 试用模式
- trial_time: 试用时长
- unbind_deduct: 解绑扣时
- bind_machine: 机器码绑定
- allow_self_unbind: 允许自行解绑
- allow_kick_off: 顶号功能
- unbind_password: 解绑密码
- remark: 项目备注
- notice: 项目公告

#### verify_custom_data（自定义数据表）
简化字段：
- 移除 template_id
- 移除 data (JSON)
- 新增 key, value, remark

### 6.3 删除的数据表
- verify_custom_templates（数据模板表）- 已废弃
- verify_custom_records（数据记录表）- 合并到 verify_custom_data
- verify_auto_issue_rules（发卡规则表）- 改为商品系统
- verify_auto_issue_records（发卡记录表）- 改为订单系统

---

## 七、API 设计变更

### 7.1 注册码管理 API 扩展

新增 API 端点（40+ 个）:
```
# 生成相关
POST   /api/codes/generate
GET    /api/codes/generate/result/:id

# 状态操作
PATCH  /api/codes/:id/freeze
PATCH  /api/codes/:id/unfreeze
PATCH  /api/codes/:id/unbind
PATCH  /api/codes/:id/renew
PATCH  /api/codes/:id/offline

# 批量操作（10个）
POST   /api/codes/batch/freeze
POST   /api/codes/batch/unfreeze
POST   /api/codes/batch/unbind
... 等

# 导入导出
POST   /api/codes/import
GET    /api/codes/export

# 清理
DELETE /api/codes/cleanup-expired
```

### 7.2 项目管理 API 扩展

新增 API 端点：
```
# 关联数据
GET    /api/projects/:id/custom-data

# 配置管理
GET    /api/projects/:id/config
PUT    /api/ /config
```

### 7.3 自定义数据 API 简化

从12个端点简化为7个：
```
GET    /api/custom-data
GET    /api/custom-data/:id
POST   /api/custom-data
PUT    /api/custom-data/:id
DELETE /api/custom-data/:id
GET    /api/custom-data/project/:projectId
```

### 7.4 新增安全策略 API

8个新端点：
```
GET    /api/security-policies
GET    /api/security-policies/:id
POST   /api/security-policies
PUT    /api/security-policies/:id
DELETE /api/security-policies/:id
GET    /api/security-policies/project/:projectId
GET    /api/security-policies/algorithms
```

### 7.5 新增商品系统 API

10个新端点：
```
# 商品管理
GET    /api/products
POST   /api/products
PUT    /api/products/:id
DELETE /api/products/:id

# 链接与购买
GET    /api/products/:id/link
GET    /api/products/public/:code
POST   /api/products/:id/purchase

# 订单与统计
GET    /api/products/orders
GET    /api/products/stats
```

### 7.6 新增黑名单管理 API

7个新端点：
```
# IP黑名单
GET    /api/blacklist/ip
POST   /api/blacklist/ip
DELETE /api/blacklist/ip/:id
POST   /api/blacklist/ip/batch

# 机器码黑名单
GET    /api/blacklist/machine
POST   /api/blacklist/machine
DELETE /api/blacklist/machine/:id
```

---

## 八、技术实现要点

### 8.1 注册码生成算法

**卡类型时长计算**:
```javascript
const cardDurations = {
  trial: 'project.trial_time', // 按项目配置
  hour: 60,                     // 分钟
  day: 24 * 60,                 // 1440分钟
  week: 7 * 24 * 60,            // 10080分钟
  month: 30 * 24 * 60,          // 43200分钟
  quarter: 90 * 24 * 60,        // 129600分钟
  half_year: 180 * 24 * 60,     // 259200分钟
  year: 365 * 24 * 60,          // 525600分钟
  permanent: 10 * 365 * 24 * 60 // 5256000分钟
};
```

**注册码格式**:
- UUID v4 生成唯一码
- 或自定义格式：项目前缀 + 随机字符
- 防重复校验

### 8.2 机器码绑定逻辑

```plaintext
1. 首次使用：
   - 获取机器码
   - 绑定到注册码
   - 设置 is_bound = true
   - 记录 activated_at

2. 后续使用：
   - 验证机器码是否匹配
   - 检查项目是否允许顶号
   - 如允许顶号：强制下线旧设备
   - 如不允许：拒绝新设备登录
```

### 8.3 安全策略实现

**加密流程**:
```plaintext
初级模式:
客户端 -> [用户密钥解密] -> [算法解密] -> 原始数据

高级模式:
客户端 -> [移除前后缀] -> [用户密钥解密] -> [算法解密(自定义标识)] -> 原始数据
```

**算法配置存储**（高级模式）:
```json
{
  "key_encrypt": "AES",
  "verify_api": {
    "algo_id": "custom_aes_256",
    "algorithm": "AES-256-CBC",
    "prefix": "VFY_",
    "suffix": "_END"
  },
  "assist_api": {
    "algo_id": "custom_sha512",
    "algorithm": "SHA-512",
    "prefix": "AST_",
    "suffix": "_FIN"
  }
}
```

### 8.4 商品购买流程

```plaintext
1. 用户访问商品链接
2. 选择商品类型和数量
3. 填写信息（或匿名）
4. 创建订单（status = pending）
5. 跳转支付
6. 支付回调
7. 验证支付成功
8. 批量生成注册码
9. 更新订单（status = paid, codes_generated）
10. 返回注册码给用户
```

---

## 九、开发影响

### 9.1 前端开发任务

新增页面：
- [ ] 安全策略管理页面（初级/高级模式切换）
- [ ] 商品管理页面
- [ ] 商品购买页面（公开页面）
- [ ] 订单管理页面

修改页面：
- [ ] 注册码生成页面（新增卡类型选择）
- [ ] 注册码列表页面（新增多个筛选和批量操作）
- [ ] 项目管理页面（新增大量配置项）
- [ ] 自定义数据页面（简化为Key-Value表单）

### 9.2 后端开发任务

新增模块：
- [ ] 安全策略模块（8个API）
- [ ] 商品系统模块（10个API）
- [ ] 黑名单管理模块（7个API）
- [ ] 支付集成模块

扩展模块：
- [ ] 注册码管理（新增40+个API）
- [ ] 项目管理（新增配置API）
- [ ] 生成批次管理

### 9.3 数据库开发任务

- [ ] 创建7个新表
- [ ] 修改2个现有表（codes, projects）
- [ ] 删除4个废弃表
- [ ] 数据迁移脚本
- [ ] 添加索引优化

### 9.4 技术栈新增

**支付集成**（需选择）:
- 支付宝
- 微信支付
- PayPal
- Stripe
- 其他第三方支付

**加密算法库**:
- crypto (Node.js 内置)
- bcryptjs
- 自定义算法实现

---

## 十、测试要点

### 10.1 功能测试

注册码管理：
- [ ] 卡类型时长计算正确性
- [ ] 批量生成性能（1000+）
- [ ] 批量操作功能
- [ ] 导入导出功能
- [ ] 机器码绑定逻辑
- [ ] 顶号功能

项目管理：
- [ ] 试用模式配置
- [ ] 解绑扣时逻辑
- [ ] 项目删除级联

安全策略：
- [ ] 初级模式加密解密
- [ ] 高级模式自定义配置
- [ ] 前后缀处理

商品系统：
- [ ] 商品链接生成
- [ ] 购买流程
- [ ] 支付回调处理
- [ ] 自动发卡逻辑

### 10.2 性能测试

- [ ] 10万+注册码的列表查询性能
- [ ] 批量操作（1000+）性能
- [ ] 并发购买处理
- [ ] 支付回调并发

### 10.3 安全测试

- [ ] 加密算法安全性
- [ ] 支付回调签名验证
- [ ] 黑名单功能有效性
- [ ] 机器码伪造防护
- [ ] 匿名购买数据隔离

---

## 十一、迁移指南

### 11.1 数据迁移

从 v1.3 升级到 v1.4：

```sql
-- 1. 备份现有数据
CREATE TABLE verify_codes_backup AS SELECT * FROM verify_codes;
CREATE TABLE verify_projects_backup AS SELECT * FROM verify_projects;

-- 2. 修改注册码表结构
ALTER TABLE verify_codes 
  ADD COLUMN card_type VARCHAR(20),
  ADD COLUMN unbind_password VARCHAR(8),
  ADD COLUMN machine_code VARCHAR(255),
  ADD COLUMN is_online BOOLEAN DEFAULT FALSE,
  ADD COLUMN is_bound BOOLEAN DEFAULT FALSE,
  ADD COLUMN last_login_ip VARCHAR(45),
  ADD COLUMN last_login_at TIMESTAMP,
  ADD COLUMN activated_at TIMESTAMP,
  ADD COLUMN expired_at TIMESTAMP,
  ADD COLUMN sale_type VARCHAR(20) DEFAULT 'author_generated',
  ADD COLUMN deleted_at TIMESTAMP;

-- 3. 修改项目表结构
ALTER TABLE verify_projects
  ADD COLUMN trial_mode VARCHAR(50),
  ADD COLUMN trial_time INT,
  ADD COLUMN unbind_deduct INT,
  ADD COLUMN bind_machine BOOLEAN DEFAULT TRUE,
  ADD COLUMN allow_self_unbind BOOLEAN DEFAULT FALSE,
  ADD COLUMN allow_kick_off BOOLEAN DEFAULT FALSE,
  ADD COLUMN unbind_password VARCHAR(8),
  ADD COLUMN remark VARCHAR(1024),
  ADD COLUMN notice VARCHAR(1024);

-- 4. 迁移自定义数据
-- 从旧的模板系统迁移到Key-Value模式
INSERT INTO verify_custom_data (project_id, key, value, remark, created_by, created_at, updated_at)
SELECT 
  project_id,
  JSON_EXTRACT(data, '$.key') as key,
  JSON_EXTRACT(data, '$.value') as value,
  '' as remark,
  created_by,
  created_at,
  updated_at
FROM verify_custom_records;

-- 5. 创建新表
CREATE TABLE verify_security_policies (...);
CREATE TABLE verify_products (...);
CREATE TABLE verify_product_types (...);
CREATE TABLE verify_orders (...);
CREATE TABLE verify_ip_blacklist (...);
CREATE TABLE verify_machine_blacklist (...);
CREATE TABLE verify_generation_batches (...);

-- 6. 删除废弃表
DROP TABLE IF EXISTS verify_custom_templates;
DROP TABLE IF EXISTS verify_custom_records;
DROP TABLE IF EXISTS verify_auto_issue_rules;
DROP TABLE IF EXISTS verify_auto_issue_records;
```

### 11.2 代码迁移

- 更新注册码生成逻辑（引入卡类型）
- 重写自定义数据API（模板系统 -> Key-Value）
- 移除发卡规则代码，实现商品系统
- 集成支付接口

---

## 十二、后续规划

### v1.5 计划
- [ ] 实现所有前端页面
- [ ] 完成所有后端API
- [ ] 支付接口集成
- [ ] 完整测试

### v2.0 展望
- 多支付方式集成
- 优惠券系统
- 会员等级体系
- 数据分析报表
- 移动端APP

---

**文档版本**: v1.4  
**更新日期**: 2025-12-12  
**维护人员**: AI Assistant

**总结**: v1.4 版本进行了全面的业务功能重构，使系统更贴近实际的注册码销售和管理需求，引入了商品化运营模式，增强了安全性和灵活性。
