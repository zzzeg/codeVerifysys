# verifySys 与按键精灵 Auth 插件统一架构与对接规划

## 1. 目标

本项目统一为一套后端、两类入口、同一批数据：

- 管理后台前端：给运营、开发者、客服使用
- Auth 插件接口：给按键精灵脚本通过 `Plugin.Auth.xxx` 调用
- 统一后端：`verifySys/nodeServer`
- 统一数据库：所有项目、注册码、策略、日志、公告、会话都落同一套表

后续云部署、本地联调、插件更新都围绕这套结构进行，不再维持两套彼此平行的客户端协议。

同时需要预留平台化能力：

- 后续允许开发者用户自行注册平台账号
- 多个开发者可同时使用同一平台
- 每个开发者只能看到和操作自己有权限的数据
- 后续可继续扩展为团队、代理、子账号、配额、计费

## 2. 统一原则

### 2.1 对外产品接口只保留一套

对按键精灵脚本的正式接口统一为：

- `Plugin.Auth.Init(serverUrl, appId, appSecret)`
- `Plugin.Auth.SetTimeout(ms)`
- `Plugin.Auth.SetIgnoreCertErrors(0/1)`
- `Plugin.Auth.GetVersion()`
- `Plugin.Auth.GetMachineCode()`
- `Plugin.Auth.Verify(licenseCode)`
- `Plugin.Auth.Heartbeat()`
- `Plugin.Auth.Logout()`
- `Plugin.Auth.TrialLogin()`
- `Plugin.Auth.Bind(licenseCode)`
- `Plugin.Auth.UnbindEx(licenseCode, unbindPassword)`
- `Plugin.Auth.GetRemainSeconds()`
- `Plugin.Auth.GetExpireTime()`
- `Plugin.Auth.GetLicenseInfo()`
- `Plugin.Auth.GetPlacard()`
- `Plugin.Auth.GetCustomData(key)`
- `Plugin.Auth.SetClientInfo(textOrJson)`
- `Plugin.Auth.ReportEvent(name, message)`
- `Plugin.Auth.GetLastStatus()`
- `Plugin.Auth.GetLastCode()`
- `Plugin.Auth.GetLastMessage()`
- `Plugin.Auth.GetLastError()`
- `Plugin.Auth.GetLastResponse()`

说明：

- `VerifySys.VerifyClient` 这套 COM 接口不再作为正式产品接口，只保留为历史参考或迁移样例。
- `SetServer(url, appId)`、`SetSecret(secret)`、`VerifyEx(url, appId, licenseCode, secret)` 可作为兼容别名保留，但新文档以 `Init(serverUrl, appId, appSecret)` 为准。
- `Bind()` 不单独定义复杂业务，本质上是“首次验证即激活并绑定”的兼容别名。
- 旧 `Unbind(licenseCode)` 参数不足，不再作为主接口，后续保留兼容即可。
- `SetIgnoreCertErrors(1)` 只允许本地联调或调试版使用，正式版默认必须校验证书。

### 2.2 后端只保留一套业务真相

所有业务规则必须在 `nodeServer` 内统一实现，前端和插件都只能调用后端，不允许各自维护一套状态判断。

必须统一的规则包括：

- 注册码是否存在
- 注册码是否属于某项目
- 注册码是否冻结、删除、过期
- 是否允许绑定机器
- 是否允许自助解绑
- 是否允许顶号
- 机器码是否匹配
- 到期时间如何计算
- 客户端请求是否通过签名和重放校验

## 3. 统一后的项目结构

建议调整为：

```text
D:\git\verifySys
├─ front\                  # 后台管理前端
├─ nodeServer\             # 唯一后端
├─ plugin-auth-dll\        # Auth.dll 源码
└─ docs\                   # 协议、架构、部署文档
```

其中：

- `front` 只负责管理界面
- `nodeServer` 负责所有业务规则、管理接口、客户端接口
- `plugin-auth-dll` 只负责机器码采集、签名、发请求、解析响应
- 按键精灵安装目录的 `plugin\` 只放编译产物，不再当源码目录

## 3.1 平台化定位

`verifySys` 不再按“单个作者自己使用的后台”设计，而按“开发者验证平台”设计。

平台角色分为两层：

- 平台层：注册、登录、用户管理、角色权限、审计
- 业务层：项目、注册码、安全策略、日志、客户端验证

这意味着后续所有业务表都要能回答两个问题：

- 这条数据归谁所有
- 谁对这条数据有访问权限

## 4. 系统边界

### 4.1 管理前端

职责：

- 登录后台
- 管理项目
- 管理注册码
- 管理安全策略
- 查看日志
- 配置公告、自定义数据

特点：

- 需要后台账号和 JWT
- 走 `/api/*` 管理接口
- 不参与注册码验证核心判定，只展示和触发操作

### 4.2 Auth 插件

职责：

- 获取机器码
- 设置服务器地址和项目标识
- 设置签名密钥
- 发起验证、绑定、解绑请求
- 缓存上次返回的状态、消息、原始响应

特点：

- 不保存业务规则
- 不直接访问数据库
- 不处理后台账号登录
- 只调用客户端验证接口

### 4.3 统一后端

职责：

- 提供管理接口
- 提供客户端接口
- 统一执行业务规则
- 统一记录日志
- 统一校验安全策略

### 4.4 数据库

职责：

- 作为唯一状态来源
- 保存项目、注册码、策略、公告、自定义数据、日志、会话、重放防护记录

## 5. 后端分层建议

当前路由里已经包含大量业务逻辑，后续需要下沉到 service 层，避免规则散落。

建议结构：

```text
nodeServer\src
├─ routes\
│  ├─ admin\              # 管理接口
│  └─ client\             # 插件接口
├─ services\
│  ├─ projectService.ts
│  ├─ licenseService.ts
│  ├─ policyService.ts
│  ├─ sessionService.ts
│  ├─ authSignService.ts
│  └─ machineService.ts
├─ repositories\          # 可选，后期引入
├─ middlewares\
└─ utils\
```

核心要求：

- 路由只负责参数校验、权限、响应包装
- 业务规则写入 service
- 后台和插件请求都调用同一套 service

## 6. 数据模型规划

## 6.0 多人平台所需的数据归属

当前系统已有 `users`、`roles`、`user_roles`，但业务表仍偏单租户思路。

为了支持开放注册和多人使用，建议补齐“所有权”和“协作关系”。

### 租户模型演进路线

第一阶段可以先使用 `owner_user_id`，快速实现“开发者只能管理自己的数据”。

第二阶段建议升级为 `workspace_id` 或 `tenant_id`：

- `user`：平台登录账号
- `workspace`：开发者个人空间或团队空间
- `project`：归属于某个 `workspace`
- `project_member`：定义用户在项目内的角色

这样后续可以自然支持：

- 一个开发者加入多个团队
- 一个团队管理多个项目
- 平台管理员代管所有租户
- 代理商管理多个客户空间
- 套餐、配额、账单按 workspace 结算

短期实现时可以先不建 `workspace` 表，但文档和字段设计要避免把所有业务永久绑定到单个 `owner_user_id`。

### 必须补充的归属字段

至少以下主表需要增加：

- `verify_projects.owner_user_id`
- `verify_register_codes.owner_user_id`
- `verify_custom_data.owner_user_id`
- `verify_security_policies.owner_user_id`

说明：

- 第一阶段可以按“项目归属”做间接控制，但项目表本身必须先有 `owner_user_id`
- `register_codes`、`custom_data`、`security_policies` 最好冗余 `owner_user_id`，方便查询、审计和以后分表

### 建议新增协作表

#### `verify_project_members`

用途：

- 支持项目协作、多成员管理

建议字段：

- `id`
- `project_id`
- `user_id`
- `role`
- `status`
- `created_at`

`role` 可先定义为：

- `owner`
- `admin`
- `operator`
- `viewer`

后续即使先不开放团队协作，这张表也建议先建，避免未来二次迁移。

### 数据权限查询原则

后端管理接口不能只依赖前端隐藏菜单，所有查询都必须在 SQL 或 service 层加入权限过滤。

推荐规则：

- 平台管理员：不过滤租户
- 开发者 owner：可管理自己拥有的项目
- 项目成员 admin/operator/viewer：按项目成员表限制权限
- 客户端插件请求：只按 `appId` 和签名识别项目，不继承后台登录权限

## 6.1 保留并继续使用的主表

- `verify_projects`
- `verify_register_codes`
- `verify_security_policies`
- `verify_custom_data`
- `verify_notifications`
- `verify_logs`

## 6.2 建议补充的表

### `verify_client_sessions`

用途：

- 保存插件客户端登录态或在线会话
- 支撑心跳、登出、顶号、在线数统计

建议字段：

- `id`
- `project_id`
- `code`
- `machine_code`
- `token`
- `status`
- `client_ip`
- `client_version`
- `created_at`
- `last_seen_at`
- `expire_at`

说明：

- 当前 `client.ts` 用内存 `Map` 保存 session，只适合本地开发，不适合正式环境。

### `verify_request_nonces`

用途：

- 防重放

建议字段：

- `id`
- `project_id`
- `nonce`
- `request_hash`
- `expire_at`
- `created_at`

### `verify_machine_blacklist`

用途：

- 封禁异常机器码

建议字段：

- `id`
- `machine_code`
- `reason`
- `status`
- `created_at`

## 6.3 用户与平台层规划

### 用户类型

平台至少要支持两类账号：

- 平台管理员
- 开发者用户

后续可扩展：

- 开发者子账号
- 代理账号
- 客服账号

### 账号生命周期

建议支持：

- 用户注册
- 邮箱验证码注册
- 登录
- 忘记密码
- 修改密码
- 禁用账号
- 审核通过后启用

### 注册后的初始资源

开发者首次注册成功后，建议自动分配：

- 默认角色
- 个人工作区
- 默认项目配额

## 6.4 数据隔离规则

平台必须默认按用户隔离数据。

规则建议如下：

1. 平台管理员可见全部数据
2. 开发者只能看到自己创建的项目
3. 开发者只能管理自己项目下的注册码、策略、日志、自定义数据
4. 插件验证请求只按 `projectId/appId` 查项目，不依赖后台登录态
5. 但管理后台操作某个项目时，必须校验当前用户是否对该项目有权限

这是后续多人共用平台时最关键的一条，必须在路由和 service 层同时落实。

## 6.5 黑名单与 IP 限制

### `verify_ip_blacklist`

用途：

- 封禁异常来源 IP

建议字段：

- `id`
- `ip`
- `reason`
- `status`
- `created_at`

## 6.6 项目与策略配置归属

### `verify_projects.config`

放非安全类配置：

- 项目展示名
- 项目公告
- UI 配置
- 脚本提示文案

### `verify_security_policies.config`

放安全与验证规则：

- `clientSecret`
- `bindMachine`
- `allowSelfUnbind`
- `allowKickOff`
- `trialMinutes`
- `maxClockSkewSec`
- `nonceTtlSec`
- `offlineGraceSec`
- `maxDevices`
- `enableResponseSign`

说明：

- 当前前端安全策略页面里配置的算法标签，可先作为扩展字段保留。
- 第一阶段不要把 AES/DES/TEA 标签强行接入主验证链路。
- 网络验证核心安全先做签名、重放防护、HTTPS、日志与权限控制。

## 6.7 项目标识体系

需要明确区分内部字段和插件暴露字段：

- `project_id`：数据库内部主键，不建议直接展示给最终脚本用户
- `project_no`：后台展示编号，方便运营识别
- `appId`：插件调用使用的项目标识
- `clientSecret`：该项目的插件签名密钥

建议：

- `appId` 全平台唯一
- `appId` 不使用项目名称
- `appId` 可由系统生成，也可允许开发者自定义一次
- `clientSecret` 与后台 JWT 密钥完全分离

这样以后迁移数据库主键、开放多租户、重命名项目，都不会影响插件侧调用。

## 6.8 配额与套餐预留

开放注册后需要预留平台配额模型。第一阶段可以不收费，但字段和业务边界要先规划。

建议配额项：

- 最大项目数
- 最大注册码数量
- 每日验证请求数
- 最大在线会话数
- 日志保留天数
- 自定义数据容量
- 每日邮件发送量

后续套餐可扩展为：

- 免费版
- 基础版
- 专业版
- 私有部署版

配额建议按 `workspace` 或 `tenant` 计算，不按单个项目计算。

## 7. 接口边界规划

## 7.1 管理接口

管理接口统一用于后台前端，全部需要后台鉴权。

建议继续使用：

- `/api/auth/*`
- `/api/projects/*`
- `/api/codes/*`
- `/api/security-policies/*`
- `/api/custom-data/*`
- `/api/notifications/*`
- `/api/logs/*`
- `/api/system/*`

管理接口职责：

- 创建项目
- 生成注册码
- 冻结、解冻、解绑、续期
- 修改安全策略
- 查看验证日志
- 设置公告和自定义数据

补充要求：

- 所有管理接口都必须基于当前登录用户做数据权限过滤
- 不能只做“前端页面上看不到”，后端查询本身就必须隔离

## 7.2 客户端接口

客户端接口单独分组，专门给 `Auth.dll` 使用，不走后台 JWT。

建议正式统一为：

- `POST /api/client/verify`
- `POST /api/client/trial-login`
- `POST /api/client/unbind`
- `POST /api/client/heartbeat`
- `POST /api/client/logout`
- `GET /api/client/placard`
- `GET /api/client/server-time`
- `GET /api/client/license-info`
- `GET /api/client/custom-data`
- `POST /api/client/client-info`
- `POST /api/client/report-event`

说明：

- 不建议正式继续维持“单路由 `op=verify/bind/unbind`”这一层抽象。
- 不建议普通客户端正式开放 `POST /api/client/custom-data` 写入接口；客户端写入类需求统一走 `SetClientInfo` 或 `ReportEvent`，由服务端按策略落库。
- 但为了兼容现有 `Auth.dll`，可以先额外提供一个过渡入口：
  - `POST /api/plugin/auth`
- 过渡期结束后，DLL 逐步切换到 `/api/client/*`。

## 7.3 推荐的插件请求字段

统一字段如下：

- `appId`
- `licenseCode`
- `machineCode`
- `timestamp`
- `nonce`
- `sign`
- `clientVersion`

说明：

- 后端内部继续可以映射到 `project_id`、`code` 等字段
- 但对插件文档要统一命名，不要同时存在 `app_id/projectToken`、`license/code` 两套叫法

### 字段命名统一表

当前代码里存在同一概念多种命名，后续需要统一。

| 概念 | 数据库字段 | 管理接口字段 | 客户端接口字段 | 说明 |
| --- | --- | --- | --- | --- |
| 项目内部 ID | `project_id` | `projectId` | 不直接使用 | 后台管理、表关联使用 |
| 插件项目标识 | `app_id` 或后续新增 `app_id` | `appId` | `appId` | 插件侧唯一项目标识，不再用 `projectToken` |
| 项目编号 | `project_no` | `projectNo` | 不使用 | 后台展示用 |
| 注册码 | `code` | `code` | `licenseCode` | 客户端协议使用 `licenseCode`，服务端内部映射到 `code` |
| 机器码 | `machine_code` | `machineCode` | `machineCode` | 客户端上传的是哈希后的机器码 |
| 到期时间 | `expire_at` | `expireAt` | `expireAt` / `expireAtText` | 时间戳用 `expireAt`，展示文本用 `expireAtText` |
| 激活时间 | `activated_at` | `activatedAt` | `activatedAt` | 毫秒时间戳 |
| 最近登录时间 | `last_login_at` | `lastLoginAt` | 不强制返回 | 管理后台展示用 |
| 最近登录 IP | `last_login_ip` | `lastLoginIp` | 不强制返回 | 管理后台展示用 |
| 销售来源 | `sale_type` | `saleType` | 不使用 | 注意不要再用 `saletype` |
| 解绑密码 | `unbind_password` | `unbindPassword` | `unbindPassword` | 客户端解绑使用 |
| 客户信息 | `customer_info` | `customerInfo` | 不使用 | 管理后台字段 |

统一原则：

- 数据库使用 snake_case
- TypeScript、前端、管理接口使用 camelCase
- 插件客户端协议也使用 camelCase
- 只在兼容旧 DLL 时临时接受 `projectToken`、`code`、`app_id`、`license`
- 兼容字段进入 service 前必须规范化为统一 DTO

### 当前需要迁移的字段

当前代码中应逐步替换：

- `projectToken` -> `appId`
- `code` -> 客户端协议中的 `licenseCode`
- `saletype` -> `saleType`
- `expiredAt` -> `expireAt`
- `projectName` 作为创建注册码入参 -> `projectId`

说明：

- 后台展示可以继续显示 `projectName`
- 后台筛选、创建、导入、批量操作应统一使用 `projectId`
- 插件请求不能依赖项目名称，因为项目名称允许修改，且多人平台下可能重名

## 7.4 公告与自定义数据作用域

公告建议分为：

- 平台公告
- 开发者公告
- 项目公告

插件 `GetPlacard(appId)` 的返回优先级：

1. 项目公告
2. 开发者公告
3. 平台公告

自定义数据第一阶段建议只做项目级：

- `project_id`
- `key`
- `value`

后续可扩展：

- 注册码级自定义数据
- 机器码级自定义数据
- 用户分组级自定义数据

## 7.5 客户端版本与协议版本

插件请求建议携带：

- `clientVersion`
- `protocolVersion`

服务端安全策略可配置：

- `minClientVersion`
- `latestClientVersion`
- `forceUpgrade`
- `upgradeMessage`

规则：

- 协议不兼容时返回明确错误码
- 老插件可在兼容期内继续使用旧接口
- 过渡接口 `/api/plugin/auth` 必须有明确下线时间

## 8. 统一业务流程

## 8.1 验证流程

`Verify(licenseCode)` 的正式流程：

1. 插件采集机器码
2. 生成时间戳与随机 `nonce`
3. 计算签名
4. 发送到 `/api/client/verify`
5. 后端校验项目、注册码、签名、时间窗、重放
6. 校验注册码状态
7. 首次验证时自动激活并绑定机器码
8. 已绑定时校验机器码是否一致
9. 写入登录日志、在线状态、会话信息
10. 返回验证结果、到期时间、可选 token

## 8.2 绑定流程

`Bind(licenseCode)` 不单独新增业务规则，定义为：

- 如果注册码未绑定，则执行与首次 `Verify` 相同的绑定逻辑
- 如果已绑定且机器码一致，则返回成功
- 如果已绑定到其他机器，则返回 `1003`

## 8.3 解绑流程

`UnbindEx(licenseCode, unbindPassword)` 的流程：

1. 校验项目是否允许自助解绑
2. 校验注册码存在且属于该项目
3. 校验解绑密码
4. 清空绑定机器码
5. 清理在线状态和会话
6. 记录日志

说明：

- 是否扣时、是否限制解绑频率，可作为第二阶段策略。

## 8.4 心跳流程

如果启用在线控制：

1. `Verify` 成功后返回会话 token
2. 插件定时调用 `/api/client/heartbeat`
3. 后端更新 `last_seen_at`
4. 若注册码被封、过期、顶下线，则返回失败

## 8.5 注册码状态机

注册码状态必须按状态机处理，不能由各接口随意改字段。

基础状态：

- `unused`：未使用
- `in_use`：已激活并可用
- `expired`：已过期
- `frozen`：已冻结
- `deleted`：已删除或不可见

允许的状态迁移：

- 首次验证成功：`unused -> in_use`
- 到期扫描或验证时发现过期：`in_use -> expired`
- 后台冻结：`unused/in_use/expired -> frozen`
- 后台恢复：`frozen -> unused` 或 `frozen -> in_use`
- 后台删除：`unused/in_use/expired/frozen -> deleted`
- 续期：`expired -> in_use` 或保持 `in_use`

规则：

- `deleted` 不允许恢复，除非后续明确增加回收站机制
- `frozen` 状态下客户端验证必须失败
- `unused` 只有首次验证或后台手动激活才能进入 `in_use`
- `expired` 续期后是否恢复为 `in_use`，由续期操作明确控制

## 8.6 并发与事务规则

客户端验证会遇到并发，尤其是同一个注册码在两台机器同时首次激活。

必须事务化的操作：

- 首次激活
- 机器码绑定
- 解绑
- 续期
- 冻结/解冻
- 顶号

建议实现规则：

- 以注册码记录为核心锁定对象
- 激活和绑定时使用数据库事务
- 关键查询使用 `SELECT ... FOR UPDATE`
- 同一个注册码并发首次绑定时，只允许一个请求成功
- 失败请求必须返回明确错误码，例如 `1003` 或 `1013`
- 写会话、写日志、更新注册码状态应在同一事务或可补偿流程内完成

建议新增错误码：

- `1013` 注册码正在被其他请求处理

## 9. 返回结构统一规范

管理接口和客户端接口建议都使用统一外壳，但客户端接口额外保留 `success`，方便 DLL 和脚本做快速判断。

管理接口统一返回：

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1780166400000
}
```

客户端接口统一返回：

```json
{
  "success": true,
  "code": 0,
  "message": "验证成功",
  "data": {
    "token": "session-token",
    "expireAt": 1782758399000,
    "expireAtText": "2026-06-30 23:59:59",
    "activatedAt": 1780166400000
  },
  "sign": "optional-response-sign",
  "timestamp": 1780166400000
}
```

规则：

- 管理接口成功码维持 `200`，便于兼容现有前端
- 客户端业务成功码固定为 `0`
- 客户端接口必须包含 `success`
- `success` 给插件快速判断
- `code` 用于精确错误处理
- `message` 用于给脚本作者和最终用户看
- `data.expireAtText` 直接兼容 DLL 的 `GetExpireTime()`
- 如果启用响应签名，返回顶层 `sign`

不建议：

- 管理接口和客户端接口混用同一套成功码
- 客户端接口只返回嵌套 `data.expireAt`，不返回可直接展示的 `expireAtText`
- 前端继续兼容 `expiredAt/expireAt` 两套字段

## 10. 错误码统一规划

- `0` 成功
- `1001` 注册码无效
- `1002` 注册码已过期
- `1003` 机器码不匹配
- `1004` 注册码已冻结
- `1005` 项目不存在
- `1006` 请求签名错误
- `1007` 时间戳无效
- `1008` 请求重复或疑似重放
- `1009` 机器码已被封禁
- `1010` IP 已被限制
- `1011` 不允许自助解绑
- `1012` 会话失效
- `1013` 注册码正在被其他请求处理
- `2001` 试用不可用或试用已用尽
- `2002` 解绑密码错误
- `-1` 网络错误

## 11. 安全规划

网络验证项目的安全重点不在“客户端做复杂加密”，而在“请求真实性、时效性、可追踪性、可控性”。

### 11.0 客户端密钥威胁模型

必须明确边界：

- 放在 DLL、脚本或客户端配置里的 `clientSecret` 都可能被逆向提取
- 请求签名不能证明客户端绝对可信
- 签名的作用是防普通伪造、防参数篡改、防重放、提高攻击成本
- 真正授权仍然依赖服务端状态、机器码绑定、风控、日志、限流和会话控制

因此不要把“有签名”理解成“客户端不可破解”。服务端必须始终把客户端输入视为不可信。

### 11.1 传输安全

- 云上正式环境必须使用 HTTPS
- 本地联调允许 HTTP
- 插件默认开启证书校验
- `SetIgnoreCertErrors(1)` 仅用于开发环境

### 11.2 请求签名

每个项目配置独立 `clientSecret`

推荐签名原文：

```text
appId + "\n" +
operation + "\n" +
licenseCode + "\n" +
machineCode + "\n" +
timestamp + "\n" +
nonce + "\n" +
clientSecret
```

签名算法：

- `HMAC-SHA256`

说明：

- 当前插件里直接 `SHA256(... + secret)` 可以先兼容
- 正式版建议切换到标准 `HMAC-SHA256`
- 服务端过渡期可以同时兼容旧签名和新签名

### 11.3 时间窗校验

- 后端校验 `timestamp`
- 默认允许误差 `300` 秒
- 超出时间窗返回 `1007`

### 11.4 防重放

- 每个请求必须带 `nonce`
- 后端保存 `nonce`，在 TTL 内不允许重复使用
- 默认 `nonceTtlSec = 600`
- 命中重复返回 `1008`

### 11.5 响应签名

建议第二阶段支持响应签名：

- 服务端对 `code/message/data/timestamp` 签名
- 插件校验后再信任结果

用途：

- 降低本地抓包伪造响应的难度

### 11.6 密钥管理

- `clientSecret` 存在 `security_policies.config`
- 后端返回策略时，对前端管理界面做脱敏展示
- 前端不得明文回显完整密钥
- 操作日志记录“谁修改了密钥”，不记录完整值

### 11.6.1 密钥轮换

每个项目建议支持密钥版本：

- `activeKey`
- `nextKey`
- `keyVersion`
- `nextKeyVersion`
- `rotateAt`

规则：

- 正常请求使用当前 active key
- 轮换期内服务端可同时接受 active key 与 next key
- 插件请求可携带 `keyVersion`
- 轮换完成后旧密钥进入废弃状态
- 后台只显示密钥指纹，不显示完整明文

第一版可以先只支持单密钥，但数据结构和接口不要阻断后续轮换。

### 11.7 密码与后台安全

- 后台账号密码继续用 `bcrypt`
- JWT `SECRET` 不与插件签名密钥混用
- 插件签名密钥与后台登录密钥必须分离
- 开放注册后必须增加邮箱验证码、找回密码、登录失败限制

### 11.7.1 开放注册下的平台安全

开放注册后至少补充：

- 邮箱验证码注册和重置密码
- 注册频率限制
- 登录失败次数限制
- 异常登录告警
- 用户状态控制（pending/active/disabled）
- 后台审计日志

如果后续接云服务器，还应补充：

- 反滥用限流
- 图形验证码或行为验证码
- IP 风险控制
- 邮件发送限速

### 11.8 风控与限流

建议增加：

- 单 IP 速率限制
- 单注册码短时间失败次数限制
- 单机器码失败次数统计
- 异常验证日志告警

限流维度建议包括：

- IP
- 项目
- 注册码
- 机器码
- 开发者账号
- 登录接口
- 邮件验证码接口

### 11.9 客户端本地缓存

如后续支持离线缓冲：

- 只允许极短时间，例如 `5~30` 分钟
- 本地缓存必须带签名和过期时间
- 不能把“永久离线可用”作为标准功能

### 11.10 机器码设计

机器码继续采用“多硬件指纹聚合后哈希”的方案：

- 网卡 MAC、适配器标识、描述、友好名称、接口类型
- CPU 信息
- BIOS 信息
- 主板信息
- 物理磁盘信息
- 固定盘卷序列
- MachineGuid
- 计算机名

要求：

- 原始指纹仅在本地参与计算
- 上传到服务端的正式值为最终哈希后的 `machineCode`
- 日志里默认记录哈希后的机器码，不记录全部原始硬件细节

### 11.11 日志分层

日志建议分为三类：

- 管理审计日志：后台用户创建、修改、冻结、解绑、改策略等操作
- 客户端验证日志：注册码、项目、机器码、IP、验证结果、错误码
- 安全事件日志：签名错误、重放请求、黑名单命中、频繁失败、异常 IP

不同日志的保留周期可以不同：

- 管理审计日志建议长期保留
- 安全事件日志建议长期保留
- 客户端验证日志可按套餐或配置保留

### 11.12 运行时依赖与 Redis

本地开发可以只用 Node.js + MySQL。

正式部署建议引入 Redis，用于：

- 客户端 session
- nonce 防重放
- 限流计数
- 登录失败计数
- 邮箱验证码
- 临时风控状态

如果第一版暂不引入 Redis，可以先用 MySQL 实现，但 service 层接口要预留替换空间。

### 11.13 数据保留与清理

需要定期清理的数据：

- 过期 nonce
- 过期 session
- 过期邮箱验证码
- 过期登录失败计数
- 长期未使用的临时风控记录

需要谨慎删除的数据：

- 管理审计日志
- 安全事件日志
- 已删除注册码
- 绑定和解绑历史

建议：

- nonce 保留 10 到 30 分钟
- session 按在线策略清理
- 客户端验证日志按套餐配置保留
- 审计日志和安全日志默认长期保留

## 12. 实施阶段规划

## 第一阶段：统一协议与主链路

目标：

- 跑通本地网络验证
- 后台能生成和管理注册码
- 插件能验证、绑定、解绑

范围：

- 统一客户端接口字段命名
- 明确 `project_id/project_no/appId/clientSecret`
- 建立注册码状态机
- 新增或调整 `/api/client/verify`
- 新增 `UnbindEx`
- 统一返回格式和错误码
- 增加签名、时间窗、nonce 校验
- 首次激活、绑定、解绑走事务
- 修正到期时间字段
- 给项目表补 `owner_user_id`

## 第二阶段：平台化账号与数据隔离

目标：

- 支持开发者注册平台并独立使用

范围：

- 注册、登录、找回密码
- 开发者角色
- 项目归属字段
- workspace/tenant 演进预留
- 数据权限过滤
- 平台管理员与开发者隔离
- 项目成员表
- 配额模型预留

## 第三阶段：在线会话与风控

目标：

- 支持心跳、登出、在线状态、顶号

范围：

- `verify_client_sessions`
- `/api/client/heartbeat`
- `/api/client/logout`
- `allowKickOff`
- 简单限流和封禁
- 客户端版本和协议版本控制

## 第四阶段：高级安全与运维

目标：

- 支持上云和长期运维

范围：

- 响应签名
- 密钥轮换
- Redis 接入
- 告警
- 操作审计
- 数据保留和清理策略
- 黑名单
- 部署文档

## 13. 当前代码改造建议

### 13.1 `verifySys` 服务端

优先调整：

- `nodeServer/src/routes/client.ts`
- 抽出 `licenseService.ts`
- 抽出 `authSignService.ts`
- 将内存 session 改为表存储或 Redis

### 13.2 `Auth.dll`

优先调整：

- `Verify` 对接正式 `/api/client/verify`
- 增加 `UnbindEx`
- 兼容 `expireAtText` 或 `data.expireAtText`
- 签名升级为 `HMAC-SHA256`

### 13.3 管理前端

优先调整：

- 注册、登录、找回密码流程预留
- 安全策略页面补充真正会生效的配置项
- 项目页面展示项目 ID、项目编号、密钥状态
- 注册码详情页展示绑定机器码、在线状态、最近验证时间
- 列表接口全部按当前用户自动过滤

## 14. 本地联调方案

本地先固定：

- 后端地址：`http://127.0.0.1:3000`
- 插件客户端地址：`http://127.0.0.1:3000/api/client/verify`
- 管理后台地址：前端开发服务器或本地构建页面
- 项目标识：先用项目 `id`
- 签名密钥：每个项目单独配置一个开发密钥

按键精灵脚本示例：

```vb
Call Plugin.Auth.SetServer("http://127.0.0.1:3000/api/client/verify", "p-1")
Call Plugin.Auth.SetSecret("dev-project-secret")

ret = Plugin.Auth.Verify("你的注册码")
If ret = 1 Then
    TracePrint "验证成功"
    TracePrint Plugin.Auth.GetExpireTime()
Else
    TracePrint Plugin.Auth.GetLastMessage()
    TracePrint Plugin.Auth.GetLastError()
End If
```

推荐新写法：

```vb
ret = Plugin.Auth.Init("http://127.0.0.1:3000", "p-1", "dev-project-secret")
If ret <> 1 Then
    TracePrint Plugin.Auth.GetLastMessage()
    ExitScript
End If

ret = Plugin.Auth.Verify("你的注册码")
If ret = 1 Then
    TracePrint "验证成功"
    TracePrint Plugin.Auth.GetExpireTime()
Else
    TracePrint Plugin.Auth.GetLastMessage()
    TracePrint Plugin.Auth.GetLastCode()
End If
```

## 15. 参考百宝云 CHM 后的接口完整性补充

参考文件：

- `D:\download\百宝云__神烦云__相关\百宝云注册码系统.CHM`
- 已解包参考目录：`D:\Program Files (x86)\按键精灵\按键精灵2014\ChmExtract_Baibao`

该 CHM 的接口设计可借鉴“覆盖范围”和“调用流程”，但不建议照搬命名、安全模型和加密接口。

### 15.1 可借鉴的设计点

百宝云接口按职责分为：

- 验证接口：初始化、注册码登录、自动登录、试用、剩余时间、详细信息、下线、通用接口
- 辅助接口：解绑、备注、更新、公告、监控、自定义数据、错误信息、机器码、版本号、充值、客户端信息
- 加密接口：MD5、AES、DES、Blowfish、TEA、RC2、RC5、RC6

其中值得吸收的规则：

- 初始化必须最先调用；初始化失败后所有服务器通信函数不可用。
- 登录成功后必须有心跳；可以提供自动心跳，但不能让开发者完全依赖自动线程。
- 所有失败都要能通过最后错误接口取到稳定错误码和错误文本。
- 公告、自定义数据、客户端信息、监控上报属于网络验证插件的常用配套能力。
- 解绑必须区分本机解绑和密码解绑，并可由项目策略控制是否允许、是否扣时。
- 机器码接口必须单独暴露，方便开发者排查用户绑定问题。

### 15.2 Auth.dll V1 必须实现的接口

第一版以“能安全完成注册码验证主链路”为目标，接口如下：

| 插件接口 | 对应后端接口 | 说明 |
| --- | --- | --- |
| `Init(serverUrl, appId, appSecret)` | 无或 `/api/client/server-time` | 初始化服务器地址、项目标识、签名密钥，可顺带探测服务器 |
| `SetTimeout(ms)` | 无 | 设置请求超时 |
| `SetIgnoreCertErrors(flag)` | 无 | 仅本地联调使用，正式版默认关闭 |
| `GetVersion()` | 无 | 返回插件版本 |
| `GetMachineCode()` | 无 | 返回本机哈希机器码 |
| `Verify(licenseCode)` | `POST /api/client/verify` | 登录验证，首次验证自动激活并绑定 |
| `Heartbeat()` | `POST /api/client/heartbeat` | 在线保活，检查冻结、过期、顶号、解绑 |
| `Logout()` | `POST /api/client/logout` | 主动下线 |
| `TrialLogin()` | `POST /api/client/trial-login` | 试用登录 |
| `UnbindEx(licenseCode, unbindPassword)` | `POST /api/client/unbind` | 本机解绑或密码解绑 |
| `GetRemainSeconds()` | 使用最近验证/心跳响应 | 返回剩余秒数，便于脚本判断 |
| `GetExpireTime()` | 使用最近验证/心跳响应 | 返回可展示的到期时间 |
| `GetLicenseInfo()` | `GET /api/client/license-info` 或最近响应 | 返回注册码状态、激活时间、到期时间等 JSON |
| `GetPlacard()` | `GET /api/client/placard` | 获取项目公告 |
| `GetCustomData(key)` | `GET /api/client/custom-data` | 获取项目级自定义数据 |
| `SetClientInfo(textOrJson)` | `POST /api/client/client-info` | 上报当前客户端信息 |
| `ReportEvent(name, message)` | `POST /api/client/report-event` | 上报异常、风控、运行状态 |
| `GetLastStatus()` | 无 | 返回最近一次调用是否成功 |
| `GetLastCode()` | 无 | 返回最近一次业务错误码 |
| `GetLastMessage()` | 无 | 返回最近一次错误文本 |
| `GetLastError()` | 无 | 兼容旧脚本，建议等同于 `GetLastMessage()` |
| `GetLastResponse()` | 无 | 返回最近一次原始 JSON 响应 |

### 15.3 Auth.dll V2 可选接口

以下能力有价值，但不进入第一版主链路：

- `VerifyAuto(licenseCode)`：验证成功后自动开启心跳线程。
- `StartHeartbeat(intervalSeconds)` / `StopHeartbeat()`：让开发者显式控制自动心跳。
- `Charge(licenseCode, cardCode)`：注册码充值或续期。
- `ClientOffline(licenseCode)`：处理异常掉线导致无法再次登录的问题。
- `CheckUpdate(currentVersion)`：只检查更新信息，不直接下载执行。
- `SetLicenseRemark(text)` / `GetLicenseRemark()`：注册码备注，适合客服排查或轻量绑定用户信息。

### 15.4 不建议照搬的接口

以下接口或能力不进入正式产品接口：

- `GetKey()`：不应向客户端暴露安全策略密钥。
- 大量对称加密工具函数：不把 AES、DES、Blowfish、TEA、RC 系列做成验证插件主功能，避免脚本作者误用。
- `CommonInterface(arg, result)`：万能接口过宽，权限、审计和兼容成本高。
- `SetMachineMode(不带网卡)`：与“机器码使用全部硬件，尤其网卡信息”的要求冲突。
- `SetMachinecode(str)`：正式版不开放自定义机器码；最多保留为内部调试开关，且编译到调试版。
- 自动更新执行器：不在验证 DLL 内直接下载并运行文件，后续只做签名校验后的更新提示或受控更新流程。

### 15.5 当前本地 client-com-dll 差距

`D:\git\verifySys\client-com-dll` 当前已有早期 COM 客户端雏形，但与最终产品接口存在差距：

- 当前 ProgID 是 `VerifySys.VerifyClient`，不是按键精灵插件目录直接调用的 `Plugin.Auth.xxx`。
- 当前接口以 `Login(projectToken, regCode, machineCode)` 返回 JSON 为主，新规划要求 `Init + Verify + 状态缓存`。
- 当前字段仍使用 `projectToken`、`regCode`，需要迁移为 `appId`、`licenseCode`。
- 当前 HTTP 请求缺少 HMAC、timestamp、nonce、防重放。
- 当前机器码只组合 CPU 名称、C 盘卷序列号、第一个 MAC，不满足“全部硬件，尤其网卡信息”的要求。
- 当前没有 `GetLastCode()`、`GetLastMessage()`、`GetLastResponse()` 这类脚本友好接口。
- 当前 `SetCustomData` 不建议继续作为普通客户端能力，应改为受控 `SetClientInfo` 或 `ReportEvent`。

### 15.6 后端需要补齐的能力

为支撑上述插件接口，后端需要新增或规范：

- `/api/client/verify`
- `/api/client/trial-login`
- `/api/client/heartbeat`
- `/api/client/logout`
- `/api/client/unbind`
- `/api/client/placard`
- `/api/client/custom-data`
- `/api/client/license-info`
- `/api/client/client-info`
- `/api/client/report-event`
- `/api/client/server-time`

所有客户端写接口必须统一经过：

- `appId` 项目识别
- `timestamp` 时间窗
- `nonce` 防重放
- `HMAC-SHA256` 签名
- `clientVersion` 和 `protocolVersion` 检查
- 限流和黑名单校验
- 统一日志记录

## 16. 最终定版

本项目正式定版如下：

- 前端后台和插件请求是两个入口
- 它们必须共用一套后端和一套数据库
- 后端按“开发者验证平台”设计，不按单人后台设计
- 插件对外接口统一为 `Plugin.Auth.xxx`
- 后端业务真相统一在 `verifySys/nodeServer`
- 安全重点先做签名、时间窗、防重放、HTTPS、日志和权限
- 平台必须预留开放注册、多人使用、数据隔离、角色权限
- 不再并行维护两套相互独立的客户端体系
