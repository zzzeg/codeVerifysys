# 后台管理系统需求文档

## 项目概述

### 项目名称
企业级后台管理系统（参考 Pure Admin）

### 项目简介
本项目旨在开发一款现代化、高性能的企业级后台管理系统，采用最新的前端技术栈，提供完善的权限管理、主题定制、国际化等功能。系统将参考 Pure Admin 的设计理念和功能特性，打造一个开箱即用、高度可配置的中后台管理解决方案。

### 技术栈

#### 前端技术栈
- **前端框架**: Vue 3（Composition API）
- **构建工具**: Vite
- **UI 组件库**: Element Plus
- **编程语言**: TypeScript
- **状态管理**: Pinia
- **CSS 框架**: Tailwind CSS
- **路由**: Vue Router
- **HTTP 客户端**: Axios
- **代码规范**: ESLint + Prettier
- **包管理器**: npm

#### 后端技术栈
本系统后端采用 **Node.js** 生态构建，根据前端功能需求选用相应的高性能、成熟稳定的库，确保系统的可靠性和可扩展性。

**核心框架与工具**:
- **运行时**: Node.js 18+ (LTS)
- **Web 框架**: Express.js（轻量级、中间件丰富）
- **编程语言**: TypeScript（类型安全，与前端保持一致）
- **包管理器**: npm（与前端统一）

**数据库与 ORM**:
- **关系型数据库**: MySQL 8.0
- **ORM 框架**: Sequelize / TypeORM / Prisma（类型安全的数据库访问）
- **Redis**: 缓存、Session 存储、消息队列（可选）
- **数据库迁移**: Knex.js / TypeORM Migrations

**数据库配置（开发环境）**:
```yaml
数据库类型: MySQL 8.0
主机地址: 127.0.0.1
端口: 3306
数据库名: dbs
用户名: root
密码:
表名前缀: verify_
字符集: utf8mb4
排序规则: utf8mb4_unicode_ci
```

**身份认证与安全**:
- **JWT 认证**: jsonwebtoken（Token 生成与验证）
- **密码加密**: bcrypt / bcryptjs（密码哈希）
- **验证码**: svg-captcha（图形验证码）
- **权限控制**: casbin（RBAC 权限模型）
- **参数校验**: joi / class-validator（请求参数验证）
- **安全中间件**: helmet（HTTP 安全头部）
- **速率限制**: express-rate-limit（接口防刷）

**文件与资源管理**:
- **文件上传**: multer（多部件表单处理）
- **图片处理**: sharp（图片压缩、裁剪、格式转换）
- **本地存储**: 文件存储在服务器本地目录
- **云存储**: @aws-sdk/client-s3（AWS S3）/ qiniu（七牛云）/ ali-oss（阿里云 OSS）**【可选，后期扩展】**
- **Excel 处理**: exceljs（Excel 导入导出）
- **PDF 生成**: pdfkit / puppeteer（PDF 文档生成）

**通信与实时功能**（可选功能，后期扩展）:
- **WebSocket**: ws / Socket.io（实时通信）**【可选】**
- **邮件服务**: nodemailer（邮件发送）**【可选】**
- **短信服务**: 阿里云 / 腾讯云短信 SDK **【可选】**

> **注意**: 以上标注为【可选】的功能涉及第三方付费服务，初始版本暂不接入，预留接口供后期扩展使用。

**日志与监控**:
- **日志**: winston / pino（结构化日志记录）
- **日志轮转**: winston-daily-rotate-file
- **性能监控**: pm2（进程管理）/ clinic.js（性能分析）
- **错误追踪**: Sentry（错误监控）**【可选】**

**任务调度与队列**:
- **定时任务**: node-cron / node-schedule
- **消息队列**: Bull（基于 Redis 的队列）

**开发与测试**:
- **开发热更新**: nodemon / ts-node-dev
- **API 文档**: Swagger / TypeDoc（自动生成 API 文档）
- **单元测试**: Jest / Mocha + Chai
- **接口测试**: Supertest
- **代码覆盖率**: nyc / c8

**工具库**:
- **日期处理**: dayjs（轻量级日期库）
- **数据验证**: validator.js（字符串验证）
- **工具函数**: lodash（实用工具库）
- **UUID 生成**: uuid
- **CORS**: cors（跨域资源共享）
- **压缩**: compression（HTTP 响应压缩）

---

## 后端 API 设计规范

### API 架构设计

#### 1. RESTful API 规范
- **资源命名**: 使用复数名词（如 `/api/users`、`/api/roles`）
- **HTTP 方法语义**:
  - `GET`: 查询资源
  - `POST`: 创建资源
  - `PUT`: 完整更新资源
  - `PATCH`: 部分更新资源
  - `DELETE`: 删除资源
- **URL 层级**: 最多 3 层嵌套（如 `/api/departments/:id/users`）
- **版本控制**: 通过 URL 前缀（如 `/api/v1/`）或请求头管理版本

#### 2. 统一响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1702304400000
}
```

**错误响应**:
```json
{
  "code": 400,
  "message": "参数验证失败",
  "errors": [
    {
      "field": "email",
      "message": "邮箱格式不正确"
    }
  ],
  "timestamp": 1702304400000
}
```

#### 3. HTTP 状态码规范
- **200**: 成功
- **201**: 创建成功
- **204**: 删除成功（无返回内容）
- **400**: 请求参数错误
- **401**: 未授权（未登录）
- **403**: 禁止访问（无权限）
- **404**: 资源不存在
- **422**: 验证失败
- **500**: 服务器内部错误

### API 端点设计

#### 认证相关 API
```
POST   /api/auth/register          # 用户注册
POST   /api/auth/login             # 用户登录
POST   /api/auth/logout            # 用户登出
POST   /api/auth/refresh-token     # 刷新 Token
POST   /api/auth/send-code         # 发送验证码
POST   /api/auth/reset-password    # 重置密码
```

#### 用户管理 API
```
GET    /api/users                  # 获取用户列表（分页、搜索、筛选）
GET    /api/users/:id              # 获取用户详情
POST   /api/users                  # 创建用户
PUT    /api/users/:id              # 更新用户
DELETE /api/users/:id              # 删除用户
PATCH  /api/users/:id/status       # 更改用户状态
POST   /api/users/:id/reset-pwd    # 重置用户密码
POST   /api/users/batch-delete     # 批量删除用户
GET    /api/users/export           # 导出用户数据
```

#### 角色权限 API
```
GET    /api/roles                  # 获取角色列表
GET    /api/roles/:id              # 获取角色详情
POST   /api/roles                  # 创建角色
PUT    /api/roles/:id              # 更新角色
DELETE /api/roles/:id              # 删除角色
GET    /api/roles/:id/permissions  # 获取角色权限
POST   /api/roles/:id/permissions  # 分配角色权限
GET    /api/permissions            # 获取权限列表（树形结构）
```

#### 菜单管理 API
```
GET    /api/menus                  # 获取菜单列表（树形结构）
GET    /api/menus/:id              # 获取菜单详情
POST   /api/menus                  # 创建菜单
PUT    /api/menus/:id              # 更新菜单
DELETE /api/menus/:id              # 删除菜单
POST   /api/menus/sort             # 菜单排序
GET    /api/menus/user             # 获取当前用户菜单（动态路由）
```

#### 部门管理 API
```
GET    /api/departments            # 获取部门列表（树形结构）
GET    /api/departments/:id        # 获取部门详情
POST   /api/departments            # 创建部门
PUT    /api/departments/:id        # 更新部门
DELETE /api/departments/:id        # 删除部门
GET    /api/departments/:id/users  # 获取部门成员
```

#### 系统日志 API
```
GET    /api/logs/operation         # 获取操作日志
GET    /api/logs/login             # 获取登录日志
GET    /api/logs/error             # 获取错误日志
GET    /api/logs/export            # 导出日志
```

#### 文件上传 API
```
POST   /api/upload/image           # 上传图片
POST   /api/upload/file            # 上传文件
POST   /api/upload/avatar          # 上传头像
DELETE /api/upload/:id             # 删除文件
```

#### 个人中心 API
```
GET    /api/profile                # 获取个人信息
PUT    /api/profile                # 更新个人信息
PUT    /api/profile/password       # 修改密码
POST   /api/profile/avatar         # 更新头像
GET    /api/profile/logs           # 获取个人操作日志
```

#### 系统配置 API
```
GET    /api/system/config          # 获取系统配置
PUT    /api/system/config          # 更新系统配置
GET    /api/system/dict/:type      # 获取数据字典
```

#### 仪表盘 API
```
GET    /api/dashboard/stats        # 获取统计数据
GET    /api/dashboard/charts       # 获取图表数据
GET    /api/dashboard/recent       # 获取最近活动
```

#### 通知消息 API
```
GET    /api/notifications          # 获取通知列表
GET    /api/notifications/unread   # 获取未读通知数
PUT    /api/notifications/:id/read # 标记为已读
POST   /api/notifications/read-all # 全部标记为已读
DELETE /api/notifications/:id      # 删除通知
```

#### 业务功能 API

**注册码管理 API**:
```
# 注册码生成
POST   /api/codes/generate              # 生成注册码（批量）
GET    /api/codes/generate/result/:id   # 获取生成结果（用于导出）

# 注册码列表与管理
GET    /api/codes                       # 获取注册码列表（支持多条件筛选和搜索）
GET    /api/codes/:id                   # 获取注册码详情
PUT    /api/codes/:id                   # 更新注册码信息（备注等）
DELETE /api/codes/:id                   # 删除注册码

# 注册码状态操作
PATCH  /api/codes/:id/freeze            # 冻结注册码
PATCH  /api/codes/:id/unfreeze          # 解冻注册码
PATCH  /api/codes/:id/unbind            # 解绑注册码
PATCH  /api/codes/:id/renew             # 续费延期
PATCH  /api/codes/:id/offline           # 强制下线

# 批量操作
POST   /api/codes/batch/freeze          # 批量冻结
POST   /api/codes/batch/unfreeze        # 批量解冻
POST   /api/codes/batch/unbind          # 批量解绑
POST   /api/codes/batch/renew           # 批量续费
POST   /api/codes/batch/delete          # 批量删除
POST   /api/codes/batch/recover         # 批量恢复
POST   /api/codes/batch/update-remark   # 批量修改备注
POST   /api/codes/batch/update-project  # 批量转换项目
POST   /api/codes/batch/blacklist-ip    # 批量添加IP到黑名单

# 导入导出
POST   /api/codes/import                # 导入注册码（Excel/TXT）
GET    /api/codes/export                # 导出注册码（Excel/TXT）

# 清理操作
DELETE /api/codes/cleanup-expired       # 清理已过期注册码

# 统计数据
GET    /api/codes/stats                 # 获取注册码统计数据
```

**项目管理 API**:
```
# 项目基础操作
GET    /api/projects                    # 获取项目列表（支持筛选搜索）
GET    /api/projects/:id                # 获取项目详情
POST   /api/projects                    # 创建项目
PUT    /api/projects/:id                # 更新项目
DELETE /api/projects/:id                # 删除项目（二次确认）

# 项目关联数据
GET    /api/projects/:id/codes          # 获取项目下所有注册码
GET    /api/projects/:id/custom-data    # 获取项目下自定义数据
GET    /api/projects/:id/stats          # 获取项目统计数据

# 项目配置
GET    /api/projects/:id/config         # 获取项目配置
PUT    /api/projects/:id/config         # 更新项目配置
```

**自定义数据 API**:
```
# 数据管理（简化的 Key-Value 模式）
GET    /api/custom-data                 # 获取自定义数据列表（支持筛选）
GET    /api/custom-data/:id             # 获取数据详情
POST   /api/custom-data                 # 创建数据
PUT    /api/custom-data/:id             # 更新数据
DELETE /api/custom-data/:id             # 删除数据

# 按项目查询
GET    /api/custom-data/project/:projectId  # 获取指定项目的自定义数据
```

**安全策略 API**:
```
# 安全策略管理
GET    /api/security-policies           # 获取安全策略列表（支持筛选）
GET    /api/security-policies/:id       # 获取策略详情
POST   /api/security-policies           # 创建安全策略
PUT    /api/security-policies/:id       # 更新安全策略
DELETE /api/security-policies/:id       # 删除安全策略

# 按项目查询
GET    /api/security-policies/project/:projectId  # 获取指定项目的安全策略

# 加密算法配置
GET    /api/security-policies/algorithms         # 获取支持的加密算法列表
```

**自动发卡（商品）API**:
```
# 商品管理
GET    /api/products                    # 获取商品列表（支持搜索筛选）
GET    /api/products/:id                # 获取商品详情
POST   /api/products                    # 创建商品
PUT    /api/products/:id                # 更新商品
DELETE /api/products/:id                # 删除商品

# 商品链接
GET    /api/products/:id/link           # 获取商品购买链接
GET    /api/products/public/:code       # 公开商品详情页面（购买页）

# 购买与支付
POST   /api/products/:id/purchase       # 创建购买订单
GET    /api/products/orders             # 获取订单列表
GET    /api/products/orders/:orderId    # 获取订单详情
POST   /api/products/payment/callback   # 支付回调接口

# 发卡统计（管理员）
GET    /api/products/stats              # 发卡统计数据
GET    /api/products/stats/project/:projectId  # 按项目统计
```

**黑名单管理 API**:
```
# IP黑名单
GET    /api/blacklist/ip                # 获取IP黑名单列表
POST   /api/blacklist/ip                # 添加IP到黑名单
DELETE /api/blacklist/ip/:id           # 从黑名单移除IP
POST   /api/blacklist/ip/batch          # 批量添加IP

# 机器码黑名单
GET    /api/blacklist/machine           # 获取机器码黑名单列表
POST   /api/blacklist/machine           # 添加机器码到黑名单
DELETE /api/blacklist/machine/:id      # 从黑名单移除机器码
```

### 数据库设计要点

#### 数据库命名规范

**表名规范**:
- 所有数据表统一使用 `verify_` 前缀
- 表名使用小写字母和下划线
- 表名应具有描述性，采用复数形式

**示例**:
- 用户表: `verify_users`
- 角色表: `verify_roles`
- 权限表: `verify_permissions`
- 用户角色关联表: `verify_user_roles`

#### 核心数据表

**用户表 (verify_users)**
- id, username, email, password, phone, avatar
- status, created_at, updated_at, last_login_at
- department_id（外键）

**角色表 (verify_roles)**
- id, name, code, description, status
- created_at, updated_at

**权限表 (verify_permissions)**
- id, name, code, type（menu/api/button）
- parent_id, path, icon, sort
- created_at, updated_at

**用户角色关联表 (verify_user_roles)**
- user_id, role_id

**角色权限关联表 (verify_role_permissions)**
- role_id, permission_id

**部门表 (verify_departments)**
- id, name, parent_id, leader_id, sort
- created_at, updated_at

**操作日志表 (verify_operation_logs)**
- id, user_id, action, module, ip, user_agent
- params, result, created_at

**登录日志表 (verify_login_logs)**
- id, user_id, ip, location, device, status
- created_at

#### 业务功能数据表

**注册码表 (verify_codes)**
- id, code, project_id, card_type, status
- unbind_password, machine_code, is_online, is_bound
- last_login_ip, last_login_at, activated_at, expired_at
- sale_type, remark
- created_by, created_at, updated_at, deleted_at

**字段说明**:
- card_type: 卡类型（trial/hour/day/week/month/quarter/half_year/year/permanent）
- status: 状态（unused/in_use/expired/frozen）
- sale_type: 销售状态（author_generated/auto_issue）
- unbind_password: 解绑密码
- machine_code: 绑定的机器码
- is_online: 是否在线（boolean）
- is_bound: 是否已绑定（boolean）

**注册码使用记录表 (verify_code_usage)**
- id, code_id, user_id, ip, device_info, location
- used_at

**项目表 (verify_projects)**
- id, name, trial_mode, trial_time, unbind_deduct
- bind_machine, allow_self_unbind, allow_kick_off
- unbind_password, remark, notice
- status, created_by, created_at, updated_at

**字段说明**:
- trial_mode: 试用模式（trial_with_time/trial_with_unbind_deduct/no_trial）
- trial_time: 试用时长（分钟）
- unbind_deduct: 解绑扣时（分钟）
- bind_machine: 是否开启机器码绑定（boolean）
- allow_self_unbind: 用户自行解绑（boolean）
- allow_kick_off: 是否顶号（boolean）
- unbind_password: 解绑密码（最大8字符）
- remark: 项目备注（最大1024字符）
- notice: 项目公告（最大1024字符）

**自定义数据表 (verify_custom_data)**
- id, project_id, key, value, remark
- created_by, created_at, updated_at

**字段说明**:
- 简化为 Key-Value 模式，不再使用模板系统
- key: 键名
- value: 键值（文本）
- remark: 备注

**安全策略表 (verify_security_policies)**
- id, project_id, status, mode
- user_secret, secret_encrypt_algo
- verify_api_encrypt_algo, verify_api_encrypt_prefix, verify_api_encrypt_suffix
- assist_api_encrypt_algo, assist_api_encrypt_prefix, assist_api_encrypt_suffix
- created_by, created_at, updated_at

**字段说明**:
- status: 策略状态（enabled/disabled）
- mode: 策略模式（basic/advanced）
- user_secret: 用户密钥（最大32字符）
- secret_encrypt_algo: 用户密钥加密算法（AES/DES）- 仅高级模式
- verify_api_encrypt_algo: 验证接口加密算法（基础：AES/DES/TEA/etc，高级：JSON配置）
- verify_api_encrypt_prefix: 加密数据前缀（最大32字符）- 仅高级模式
- verify_api_encrypt_suffix: 加密数据后缀（最大32字符）- 仅高级模式
- assist_api_encrypt_algo: 辅助接口加密算法（基础：MD5/SHA1/etc，高级：JSON配置）
- assist_api_encrypt_prefix: 加密数据前缀
- assist_api_encrypt_suffix: 加密数据后缀

**商品表 (verify_products)**
- id, project_id, name, description, summary
- allow_anonymous, min_quantity, max_quantity
- product_link_code, status
- created_by, created_at, updated_at

**字段说明**:
- allow_anonymous: 允许匿名购买（boolean）
- min_quantity: 单次最少购买数量
- max_quantity: 单次最多购买数量
- product_link_code: 商品链接唯一码
- description: 商品描述（最大1024字符）

**商品类型价格表 (verify_product_types)**
- id, product_id, type_name, price, card_type
- sort, created_at, updated_at

**字段说明**:
- type_name: 商品类型名称（自定义）
- price: 价格（decimal）
- card_type: 卡类型（hour/day/week/month/etc）
- sort: 排序

**订单表 (verify_orders)**
- id, product_id, product_type_id, user_id
- quantity, total_price, status, payment_method
- payment_no, payment_time
- codes_generated, buyer_info
- created_at, updated_at

**字段说明**:
- status: 订单状态（pending/paid/cancelled/finished）
- payment_method: 支付方式
- payment_no: 支付流水号
- codes_generated: 生成的注册码列表（JSON）
- buyer_info: 买家信息（JSON，匿名购买时为空）

**IP黑名单表 (verify_ip_blacklist)**
- id, ip, reason, created_by
- created_at, updated_at

**机器码黑名单表 (verify_machine_blacklist)**
- id, machine_code, reason, created_by
- created_at, updated_at

**生成批次记录表 (verify_generation_batches)**
- id, project_id, card_type, quantity
- batch_code, codes_data
- created_by, created_at

**字段说明**:
- batch_code: 批次编号（用于导出）
- codes_data: 生成的注册码数据（JSON）
- 用于记录每次批量生成的注册码，方便导出

### 中间件设计

#### 必需中间件
1. **身份认证中间件**: 验证 JWT Token
2. **权限验证中间件**: 基于 RBAC 的权限检查
3. **参数验证中间件**: 统一参数校验
4. **日志记录中间件**: 记录操作日志
5. **错误处理中间件**: 统一错误响应
6. **限流中间件**: 防止接口恶意调用
7. **CORS 中间件**: 跨域处理
8. **请求压缩中间件**: 响应数据压缩

### 安全措施

1. **SQL 注入防护**: 使用 ORM 参数化查询
2. **XSS 防护**: 对用户输入进行转义
3. **CSRF 防护**: 使用 Token 验证
4. **密码安全**: bcrypt 加密 + 盐值
5. **敏感信息加密**: AES 加密存储
6. **接口签名**: 防止数据篡改
7. **访问频率限制**: 防止暴力破解
8. **HTTPS**: 生产环境强制使用 HTTPS

---

## 核心功能需求

### 1. 用户认证与授权

#### 1.1 登录系统
- **功能描述**: 用户通过用户名/邮箱和密码登录系统
- **需求细节**:
  - 支持账号密码登录
  - 支持记住密码功能
  - 登录失败提示错误信息
  - 支持验证码（图形验证码/滑块验证）
  - Token 认证机制（JWT）
  - 自动刷新 Token
  - 登录状态持久化

#### 1.2 用户注册
- **功能描述**: 新用户注册账号
- **需求细节**:
  - 用户名、邮箱、密码输入
  - 密码强度校验
  - 邮箱验证码验证
  - 用户协议勾选

#### 1.3 密码管理
- **功能描述**: 用户密码找回和修改
- **需求细节**:
  - 忘记密码功能（邮箱验证）
  - 修改密码功能
  - 密码加密存储

#### 1.4 权限管理（RBAC）
- **功能描述**: 基于角色的访问控制系统
- **需求细节**:
  - 用户-角色-权限三层模型
  - 角色管理（增删改查）
  - 权限配置（菜单权限、接口权限、按钮权限）
  - 动态路由生成
  - 权限指令（v-permission）
  - 细粒度权限控制

#### 1.5 系统角色定义

##### 1.5.1 管理员角色（Administrator）
- **角色标识**: `admin`
- **角色描述**: 系统超级管理员，拥有所有权限
- **权限范围**: 
  - 用户管理: 查看、创建、编辑、删除所有用户
  - 角色管理: 管理所有角色和权限分配
  - 菜单管理: 配置系统菜单结构
  - 部门管理: 管理组织架构
  - 系统配置: 修改系统参数和配置
  - 系统日志: 查看所有操作日志
  - 业务功能: 完全访问所有业务功能页面
  
- **可访问页面**:
  - ✅ 所有系统页面（无限制）
  - ✅ 用户权限控制台（可以控制其他用户能看到哪些界面）
  
- **特殊权限**:
  - 分配和修改其他用户的角色
  - 创建和删除角色
  - 配置菜单和权限
  - 查看所有用户的操作记录

##### 1.5.2 普通用户角色（User）
- **角色标识**: `user`
- **角色描述**: 默认注册用户，拥有基础业务功能权限
- **权限范围**: 仅限业务功能操作，无系统管理权限
- **默认分配**: 新用户注册时自动分配此角色

- **可访问页面**:
  - ✅ 注册码生成
  - ✅ 注册码列表管理
  - ✅ 项目管理
  - ✅ 自定义数据
  - ✅ 自动发卡
  - ✅ 个人中心
  
- **受限功能**:
  - ❌ 用户管理（无法查看其他用户）
  - ❌ 角色管理（无法管理角色）
  - ❌ 菜单管理（无法配置菜单）
  - ❌ 部门管理（无法管理部门）
  - ❌ 系统配置（无法修改系统参数）
  - ❌ 系统日志（仅能查看自己的操作记录）

##### 1.5.3 角色切换与管理
- **角色提升**: 仅管理员可以将普通用户提升为管理员
- **角色降级**: 管理员可以将其他管理员降级为普通用户（至少保留一个管理员）
- **多角色**: 暂不支持，每个用户仅能拥有一个角色
- **角色审计**: 所有角色变更都会记录在操作日志中

---

### 2. 系统布局

#### 2.1 布局模式
- **功能描述**: 支持多种布局模式，用户可自由切换
- **需求细节**:
  - **左侧菜单布局**: 经典左侧导航模式
  - **顶部菜单布局**: 顶部导航栏模式
  - **混合布局**: 顶部+左侧双导航
  - **双栏布局**: 侧边栏分为两栏显示
  - 布局配置持久化保存
  - 响应式布局适配（PC/平板/手机）

#### 2.2 导航组件
- **顶部导航栏**:
  - Logo 展示
  - 面包屑导航
  - 工具栏（全屏、刷新、通知、语言切换）
  - 用户信息下拉菜单
  - 搜索功能

- **侧边菜单**:
  - 多级菜单展示
  - 菜单折叠/展开
  - 菜单搜索
  - 菜单图标
  - 菜单徽章（Badge）
  - 当前菜单高亮

#### 2.3 标签页（Tabs）
- **功能描述**: 多页签管理，类似浏览器标签页
- **需求细节**:
  - 打开新标签页
  - 关闭标签页（当前/其他/所有/左侧/右侧）
  - 标签页右键菜单
  - 标签页拖拽排序
  - 标签页刷新
  - 标签页持久化

---

### 3. 主题系统

#### 3.1 主题配置
- **功能描述**: 丰富的主题定制功能
- **需求细节**:
  - 明亮/暗黑模式切换
  - 主题色自定义（颜色选择器）
  - 预设主题方案
  - 侧边栏主题切换
  - 顶部导航主题切换
  - 灰色模式（哀悼模式）
  - 色弱模式
  - 主题配置实时预览
  - 主题配置保存

#### 3.2 界面设置
- **功能描述**: 界面细节自定义
- **需求细节**:
  - 侧边栏宽度调整
  - 字体大小调整
  - 圆角大小设置
  - 动画效果开关
  - 页面加载进度条

---

### 4. 国际化（i18n）

#### 4.1 多语言支持
- **功能描述**: 系统支持多语言切换
- **需求细节**:
  - 支持中文简体
  - 支持中文繁体
  - 支持英文
  - 语言切换器（下拉菜单）
  - 语言配置持久化
  - 组件库国际化同步
  - 自定义国际化文案

#### 4.2 语言管理
- **功能描述**: 后台语言管理功能
- **需求细节**:
  - 语言包在线编辑
  - 翻译缺失提示
  - 语言包导入导出

---

### 5. 路由系统

#### 5.1 路由配置
- **功能描述**: 灵活的路由配置系统
- **需求细节**:
  - 静态路由
  - 动态路由（基于权限）
  - 路由懒加载
  - 路由元信息配置
  - 嵌套路由支持
  - 外链支持

#### 5.2 路由守卫
- **功能描述**: 路由访问控制
- **需求细节**:
  - 登录状态验证
  - 权限校验
  - 页面标题动态设置
  - 页面加载进度条
  - 路由白名单

---

### 6. 功能页面

#### 6.1 首页/仪表盘
- **功能描述**: 数据概览和统计展示
- **需求细节**:
  - 数据卡片（用户数、订单数、销售额等）
  - 数据图表（折线图、柱状图、饼图）
  - 待办事项列表
  - 快捷入口
  - 实时数据更新
  - 可拖拽布局

#### 6.2 用户管理
- **功能描述**: 系统用户管理
- **需求细节**:
  - 用户列表（表格展示）
  - 用户搜索（姓名、邮箱、手机号）
  - 用户筛选（角色、状态、部门）
  - 用户新增/编辑/删除
  - 用户详情查看
  - 批量操作（批量删除、批量导出）
  - 用户状态管理（启用/禁用）
  - 重置密码
  - 分配角色
  - 分页加载

#### 6.3 角色管理
- **功能描述**: 角色权限管理
- **需求细节**:
  - 角色列表
  - 角色新增/编辑/删除
  - 权限分配（树形结构）
  - 角色成员查看
  - 角色状态管理

#### 6.4 系统日志
- **功能描述**: 系统操作记录
- **需求细节**:
  - **操作日志**: 用户操作记录（增删改查）
  - **登录日志**: 用户登录/登出记录
  - **错误日志**: 系统异常记录
  - 日志搜索和筛选
  - 日志导出
  - 日志详情查看

---

#### 6.7 注册码管理

##### 6.7.1 注册码生成
- **功能描述**: 生成系统注册码，用于用户激活或授权
- **权限要求**: 普通用户及以上
- **需求细节**:
  - **绑定项目**: 选择项目后，根据对应项目可生成注册码
  - **批量生成**: 支持一次生成多条注册码（选择条数）
  - **有效期设置**: 选择注册码有效期类型（小时卡、天卡、周卡、月卡、季卡、半年卡、年卡、永久(实际使用时间为10年)，每天24小时，每周7天，每个月30个自然日算，以此类推）
  - **备注信息**: 可为注册码添加备注说明
  - **生成记录**: 记录注册码生成时间、生成人(生成人普通用户不可见，管理员可见所有用户的生成记录)
  - **导出功能**: 生成后显示当次所有生成的注册码(表格显示：注册码、项目名称、卡类型、备注)，并显示"导出"按钮，支持导出当次生成的所有注册码（Excel/txt）

##### 6.7.2 注册码列表
- **功能描述**: 管理所有生成的注册码
- **权限要求**: 普通用户及以上
- **需求细节**:
  - **列表展示**: 表格形式展示所有注册码，栏目：
    - 复选框(单选/全选)、注册码、卡类型、项目名称、状态(未使用/使用中/已过期/已冻结)、激活时间/到期时间/最后访问时间、是否在线、备注、解绑密码、操作(解绑/未绑、冻结、续费、下线/离线、删除)
  - **状态筛选**: 按状态筛选（单选框：未使用/使用中/已过期/已删除/已冻结）、卡类型筛选（单选框：试用卡、小时卡、天卡、周卡、月卡、季卡、半年卡、年卡、永久）、项目名称筛选(下拉选择：所有项目、读取用户创建的项目名称...)、备注筛选(输入框：模糊查询)、在线状态筛选(下拉选择：所有/在线/离线)、销售状态:(下拉选择：所有、作者生成、自动发卡)
  - **搜索功能**: 支持按注册码、机器码、选择时间范围(时间类型下拉选择：选择时间/激活时间/到期时间/最后登录时间)搜索
  - **详情查看**: 查看注册码详细信息和使用记录，底部弹窗显示
    - 注册码、项目名称、解绑密码、卡类型、创建时间、激活时间、到期时间、最后登录时间、状态、是否在线、登录IP地址、是否绑定、机器码、使用者信息、备注(可修改)
  - **状态管理**: 
    - 禁用注册码(冻结)
    - 启用注册码(解冻)
    - 延长有效期(续费)
    - 下线/离线
    - 删除注册码
  - **批量操作**: 冻结/解冻、解绑、续费、删除、恢复、导入(txt/excel)、导出(txt/excel)、清理已过期、修改备注、修改项目类型、添加ip到黑名单、转换(到别的)项目
  - **分页加载**: 支持大数据量分页展示

#### 6.8 项目管理
- **功能描述**: 管理业务项目，创建并且选择项目后才可生成注册码
- **权限要求**: 普通用户及以上
- **需求细节**:
  - **项目列表**: 展示所有项目
    - 项目名称、试用模式(试用<试用时间、解绑扣时>/禁止试用)、是否绑定机器(x/√)、用户自行解绑(x/√)、公告信息、操作(编辑、删除)、管理(注册码、自定义数据)
  - **项目创建**: 新建项目
    - 项目名称（必填）
    - 试用模式（选择：试用<试用时间、解绑扣时>/禁止试用）
    - 开启机器码绑定（单选：启用/关闭）
    - 用户自行解绑（单选：启用/关闭）
    - 是否顶号：（单选：启用/关闭）开启功能 (同一个注册码在新的地方登陆,旧的地方的注册码会掉线;关闭功能,一个注册码登陆后,就不能在其他地方登陆,调用下线函数或者在注册码管理界面让注册码下线,就可以重新登陆了)
    - 解绑密码：(输入框,最大8个字符)
    - 项目备注：(输入框,项目备注最长不能超过1024个字符)
    - 项目公告：(输入框,项目公告最长不能超过1024个字符)
  - **项目编辑**: 修改项目信息，与"项目创建"内容一致
  - **项目删除**: 删除项目（需二次确认，关联注册码处理，删除项目后，项目下所有注册码将被删除，项目下所有自定义数据将被删除）
  - **项目统计**: 
    - 项目下注册码总数
    - 已使用注册码数
    - 未使用注册码数
    - 已过期注册码数
  - **关联管理**: 查看项目下所有注册码、自定义数据
  - **搜索筛选**: 按项目名称(下拉选择)、项目备注(模糊查询)、项目公告(模糊查询)

#### 6.9 自定义数据管理
- **功能描述**: 管理用户自定义的数据，绑定项目，根据项目创建自定义数据，表格显示(带分页功能)
- **权限要求**: 普通用户及以上
- **需求细节**:
  - **数据列表**: 
    - 项目名称、key值、value值、备注、操作(编辑、删除)
  - **数据创建**: 
    - 选择项目(必选)、key值(必填)、value值(必填)、备注(可选)
  - **数据编辑**: 
    - 修改key值、value值、备注
  - **数据删除**: 
    - 删除数据（需二次确认）
  - **搜索筛选**: 按项目名称(下拉选择)、key值(模糊查询)、备注(模糊查询)

#### 6.10 安全策略管理
- **功能描述**: 管理用户的安全策略，绑定项目，根据项目创建安全策略，表格显示(带分页功能)
- **权限要求**: 普通用户及以上
- **需求细节**:
  - **安全策略列表**: 
    - 项目名称、策略状态(开启/关闭)、策略模式、创建时间、操作(编辑、删除)
  - **添加安全策略**: 
    - 选择项目(必选)、
    - 策略状态(单选:开启/关闭，开启时才显示后面的所有功能)
    - 策略模式(单选:初级/高级)
    - 初级模式：
      - 用户密钥(客户端解密通讯数据所使用的密钥，输入框，最大32个字符)
      - 验证接口安全策略:通讯加密(客户端根据算法标识使用对应的解密算法，下拉选择框：AES/DES/TEA/RC2/RC5/RC6/BLOWFISH)
      - 辅助接口安全策略:数据加密(客户端根据算法标识使用对应的解密算法，下拉选择框：MD5/SHA1/SHA256/SHA384/SHA512)
    - 高级模式：
      - 用户密钥(客户端解密通讯数据所使用的密钥，输入框，最大32个字符)
      - 用户密钥加密(客户端根据算法标识使用对应的解密算法，下拉选择框：AES/DES)
      - 验证接口安全策略:
        - 通讯加密(列出所有加密算法，并且让用户自定义算法标识)
        - 加密后数据前缀(输入框，用户可自定义，最大32个字符)
        - 加密后数据后缀(输入框，用户可自定义，最大32个字符)
      - 辅助接口安全策略:
        - 通讯加密(列出所有加密算法，并且让用户自定义算法标识)
        - 加密后数据前缀(输入框，用户可自定义，最大32个字符)
        - 加密后数据后缀(输入框，用户可自定义，最大32个字符)
    - 用户密钥(客户端解密通讯数据所使用的密钥)
  - **编辑安全策略**: 
    - 同"添加安全策略"内容
  - **删除安全策略**: 
    - 删除数据（需二次确认）
  - **搜索筛选**: 按项目名称(下拉选择)、策略模式(下拉选择)、策略状态(下拉选择)


#### 6.11 自动发卡
- **功能描述**: 自动化发放注册码功能，生成一个链接，用户点击连接跳转到对应商品页面，购买注册码，用户点击链接购买付款成功后，后台自动生成对应期限的注册码返回给用户
- **权限要求**: 普通用户及以上
- **需求细节**:
  - **商品添加**:
    - 选择发放的项目
    - 输入商品名称
    - 简介
    - 匿名购买(单选:启用/关闭，启用后可以匿名购买，否则需要注册账户才能购买)
    - 商品类型/价格：表格形式，可以添加/编辑/删除 一行表格，一行为一个商品类型/价格
      - 商品类型(输入框，自定义显示的商品类型名，类似淘宝购买界面的商品类型)
      - 价格(必填)
      - 卡类型（下拉选择：小时卡、天卡、周卡、月卡、季卡、半年卡、年卡、永久卡）
      - 操作栏(删除一行)
    - 设置单次发放数量(一行内显示：单次最少购买数量，单次最多购买数量)
    - 商品描述(输入框，最大1024个字符，可以使用换行符)
  - **商品列表管理**: 
    - 商品列表，表格形式显示，带分页功能（栏目：项目名称、商品名称、购买数(最少/最多)、允许匿名购买(允许/不允许)、商品链接(生成可直接点击跳转的链接)、商品简介、操作(编辑、删除)）
  - **商品删除**: 
    - 删除商品（需二次确认）
  - **商品编辑**: 
    - 编辑商品
  - **商品搜索**: 商品名称(模糊查询)、项目名称(下拉选择)
  - **发卡统计**: (仅管理员可见)
    - 总发卡次数
    - 总发卡数量
    - 按项目统计
    - 按时间统计

---

### 7. 通用功能

#### 7.1 表格组件
- **功能描述**: 增强型数据表格
- **需求细节**:
  - 分页加载
  - 排序功能
  - 筛选功能
  - 列显示/隐藏
  - 列宽调整
  - 行选择（单选/多选）
  - 表格导出（Excel/CSV）
  - 自定义列渲染
  - 树形表格
  - 固定列

#### 7.2 表单组件
- **功能描述**: 强大的表单功能
- **需求细节**:
  - 表单验证
  - 异步验证
  - 自定义验证规则
  - 表单重置
  - 表单回显
  - 动态表单
  - 表单联动

#### 7.3 文件上传
- **功能描述**: 文件上传管理
- **需求细节**:
  - 单文件上传
  - 多文件上传
  - 拖拽上传
  - 文件预览
  - 文件大小限制
  - 文件类型限制
  - 上传进度显示
  - 图片裁剪

#### 7.4 富文本编辑器
- **功能描述**: 富文本内容编辑
- **需求细节**:
  - 文本格式化
  - 图片插入
  - 表格插入
  - 代码高亮
  - 全屏编辑
  - Markdown 支持

#### 7.5 消息通知
- **功能描述**: 系统消息和通知
- **需求细节**:
  - 消息提示（Message）
  - 通知（Notification）
  - 消息盒子（MessageBox）
  - 消息中心
  - 未读消息提醒
  - 消息分类（系统通知/待办事项）
  - 消息已读/未读状态

#### 7.6 搜索功能
- **功能描述**: 全局搜索
- **需求细节**:
  - 菜单搜索（快捷键支持）
  - 搜索历史记录
  - 搜索结果高亮
  - 模糊搜索

---

### 8. 系统设置

#### 8.1 个人中心
- **功能描述**: 用户个人信息管理
- **需求细节**:
  - 个人信息查看/编辑
  - 头像上传
  - 修改密码
  - 绑定邮箱/手机
  - 操作日志查看
  - 安全设置

#### 8.2 系统配置
- **功能描述**: 系统参数配置
- **需求细节**:
  - 网站基本信息
  - LOGO 配置
  - 文件上传配置
  - 邮件服务器配置
  - 第三方登录配置
  - 系统参数管理

---

### 9. 数据可视化

#### 9.1 图表组件
- **功能描述**: 基于 ECharts 的数据可视化
- **需求细节**:
  - 折线图
  - 柱状图
  - 饼图
  - 雷达图
  - 地图
  - 仪表盘
  - 热力图
  - 图表主题切换（跟随系统主题）
  - 图表响应式

#### 9.2 数据大屏
- **功能描述**: 全屏数据展示
- **需求细节**:
  - 全屏模式
  - 自适应布局
  - 实时数据更新
  - 炫酷动画效果

---

## 非功能需求

### 1. 性能要求
- 首屏加载时间 < 2 秒
- 路由切换流畅，无卡顿
- 支持 10000+ 数据量表格（虚拟滚动）
- 内存占用合理，无内存泄漏
- 打包体积优化（gzip < 500KB）

### 2. 安全要求
- XSS 防护
- CSRF 防护
- SQL 注入防护
- 敏感信息加密
- 接口权限验证
- 操作审计日志
- 定期安全扫描

### 3. 兼容性要求
- 浏览器兼容：Chrome 90+、Edge 90+、Firefox 88+、Safari 14+
- 移动端适配：响应式布局，支持主流移动设备
- 屏幕分辨率：最低支持 1366x768

### 4. 可维护性
- 代码规范统一（ESLint + Prettier）
- TypeScript 类型覆盖率 > 90%
- 组件文档完善
- 单元测试覆盖率 > 70%
- Git 提交规范（Conventional Commits）

### 5. 可扩展性
- 模块化设计，低耦合
- 插件化架构
- 可配置化（通过配置文件自定义功能）
- 主题可扩展
- 组件可扩展

---

## 开发计划

### 第一阶段：基础框架搭建（2 周）

**前端任务**:
- [ ] 项目初始化和技术栈配置
- [ ] 基础布局开发（4 种布局模式）
- [ ] 路由系统搭建
- [ ] 状态管理配置
- [ ] HTTP 请求封装

**后端任务**:
- [ ] Node.js 项目初始化（TypeScript 配置）
- [ ] Express/Koa 框架搭建
- [ ] 数据库设计与 ORM 配置
- [ ] Redis 缓存配置
- [ ] JWT 认证机制实现
- [ ] 中间件开发（认证、日志、错误处理）
- [ ] API 基础架构搭建
- [ ] 登录/注册接口开发
- [ ] API 文档生成（Swagger）

### 第二阶段：核心功能开发（3 周）

**前端任务**:
- [ ] 用户管理页面
- [ ] 角色管理页面
- [ ] 菜单管理页面
- [ ] 部门管理页面
- [ ] 主题系统开发
- [ ] 权限指令开发

**后端任务**:
- [ ] RBAC 权限系统实现（Casbin）
- [ ] 用户管理 API
- [ ] 角色管理 API
- [ ] 菜单管理 API
- [ ] 部门管理 API
- [ ] 动态路由生成接口
- [ ] 权限验证中间件
- [ ] 操作日志记录

### 第三阶段：通用组件开发（2 周）

**前端任务**:
- [ ] 表格组件封装
- [ ] 表单组件封装
- [ ] 文件上传组件
- [ ] 富文本编辑器集成
- [ ] 图表组件封装
- [ ] 消息通知组件

**后端任务**:
- [ ] 文件上传 API（Multer + Sharp）
- [ ] 云存储集成（OSS/S3）
- [ ] Excel 导入导出功能
- [ ] PDF 生成功能
- [ ] 邮件发送服务（Nodemailer）
- [ ] 短信发送服务

### 第四阶段：功能完善（2 周）

**前端任务**:
- [ ] 国际化支持
- [ ] 系统日志页面
- [ ] 个人中心
- [ ] 系统配置页面
- [ ] 数据大屏
- [ ] 移动端适配

**后端任务**:
- [ ] WebSocket 实时通信（Socket.io）
- [ ] 系统日志 API
- [ ] 个人中心 API
- [ ] 系统配置 API
- [ ] 数据字典管理
- [ ] 定时任务调度（node-cron）
- [ ] 消息队列（Bull）

### 第五阶段：测试和优化（1 周）

**前端任务**:
- [ ] 组件单元测试（Jest）
- [ ] E2E 测试
- [ ] 性能优化（打包体积、加载速度）
- [ ] 浏览器兼容性测试
- [ ] 前端文档完善

**后端任务**:
- [ ] API 单元测试（Jest）
- [ ] 接口集成测试（Supertest）
- [ ] 性能测试与优化
- [ ] 安全测试（SQL 注入、XSS、CSRF）
- [ ] 压力测试
- [ ] API 文档完善
- [ ] 部署文档编写

---

## 交付物

### 1. 代码交付
- 完整的前端源代码（Vue 3 + TypeScript）
- 完整的后端源代码（Node.js + TypeScript）
- 数据库设计文档与 SQL 脚本
- 数据库迁移文件
- 环境配置文件说明（.env.example）
- Docker 配置文件（Dockerfile + docker-compose.yml）

### 2. 文档交付
- 系统部署文档
- 开发文档
- API 接口文档
- 用户使用手册
- 常见问题 FAQ

### 3. 其他交付
- 设计稿（UI 设计）
- 项目演示视频
- 测试报告

---

## 参考资料

### 前端参考
- [Pure Admin 官方网站](https://pure-admin.github.io/vue-pure-admin/)
- [Pure Admin 文档](https://pure-admin.cn/)
- [Vue 3 官方文档](https://vuejs.org/)
- [Element Plus 文档](https://element-plus.org/)
- [Vite 官方文档](https://vitejs.dev/)
- [Pinia 文档](https://pinia.vuejs.org/)
- [Tailwind CSS 文档](https://tailwindcss.com/)

### 后端参考
- [Node.js 官方文档](https://nodejs.org/)
- [Express.js 文档](https://expressjs.com/)
- [TypeScript 官方文档](https://www.typescriptlang.org/)
- [Sequelize 文档](https://sequelize.org/)
- [TypeORM 文档](https://typeorm.io/)
- [Prisma 文档](https://www.prisma.io/)
- [JWT 文档](https://jwt.io/)
- [Casbin 文档](https://casbin.org/)
- [Socket.io 文档](https://socket.io/)
- [Swagger 文档](https://swagger.io/)

---

## 版本历史

| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|----------|
| v1.0 | 2025-12-11 | AI Assistant | 初始版本创建 |
| v1.1 | 2025-12-11 | AI Assistant | 添加 Node.js 后端技术栈与 API 设计规范 |
| v1.2 | 2025-12-11 | AI Assistant | 明确数据库配置、表命名规范（verify_前缀）、标注可选功能 |
| v1.3 | 2025-12-12 | AI Assistant | 删除高级功能、新增业务功能（注册码、项目、自定义数据、自动发卡）、完善角色权限定义 |
| v1.4 | 2025-12-12 | AI Assistant | 重构业务功能需求、完善注册码管理、项目配置、新增安全策略和商品系统 |

---

## 附录

### A. 术语表
- **RBAC**: 基于角色的访问控制（Role-Based Access Control）
- **JWT**: JSON Web Token，一种身份验证机制
- **i18n**: 国际化（Internationalization）
- **XSS**: 跨站脚本攻击（Cross-Site Scripting）
- **CSRF**: 跨站请求伪造（Cross-Site Request Forgery）

### B. 技术选型说明

**前端技术选型理由**:
- **Vue 3**: 采用 Composition API，更好的 TypeScript 支持，性能更优
- **Vite**: 构建速度快，开发体验好，原生 ESM 支持
- **Element Plus**: Vue 3 版本的 Element UI，组件丰富，企业级 UI 组件库
- **TypeScript**: 类型安全，降低运行时错误，提升代码可维护性
- **Pinia**: Vue 官方推荐的状态管理库，比 Vuex 更简单轻量
- **Tailwind CSS**: 实用优先的 CSS 框架，快速构建界面，高度可定制

**后端技术选型理由**:
- **Node.js**: 前后端统一语言（JavaScript/TypeScript），提高开发效率
- **Express/Koa**: 成熟稳定的 Web 框架，中间件生态丰富，性能优异
- **TypeScript**: 与前端保持技术栈一致，类型安全，降低 Bug 率
- **Sequelize/TypeORM**: 成熟的 ORM 框架，支持多种数据库，提供类型安全
- **JWT**: 无状态认证，适合分布式系统，前后端分离的最佳实践
- **Redis**: 高性能缓存，支持多种数据结构，适合 Session、队列等场景
- **bcrypt**: 业界标准的密码加密算法，安全性高
- **Casbin**: 强大的访问控制库，支持多种权限模型
- **Socket.io**: 可靠的 WebSocket 库，支持降级方案，浏览器兼容性好

---

**文档结束**
